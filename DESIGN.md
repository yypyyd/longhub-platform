# LongHub 平台总体设计

> 状态：有效  
> 基线：LongHub Desktop 0.4.1 / OpenClaw 2026.7.1-2  
> 更新日期：2026-07-30

## 产品定位

龙枢是企业可管理、可分发、可运营的多智能体平台。Windows 客户端直接提供经过产品化约束的
OpenClaw 原生聊天体验；用户可以一键切换已授权的不同功能智能体，模型、助手配置、授权、
能力包和版本策略由龙枢后台统一控制。

### 目标

- 终端用户安装后直接聊天，不配置 Gateway、Provider、API Key 或模型。
- 用户在同一客户端内一键切换不同功能智能体，每个智能体拥有独立身份、会话、记忆和能力。
- 保留 OpenClaw 原生 WebUI 的主要能力与上游升级价值，不重复建设聊天壳。
- 用户已有 OpenClaw 与龙枢内置实例可以同时运行，身份、状态、工作区和端口互不影响。
- 企业管理员可以统一管理模型、助手、设备、能力、额度和发布策略。
- 每次客户端和 OpenClaw 升级都可验证、可灰度、可回滚。

### 非目标

- 不允许客户端直接持有真实上游模型 API Key。
- 不向普通用户暴露模型选择、Gateway、Token 或基础设施配置。
- 不长期维护一份完整的 OpenClaw WebUI 分叉。
- 不让用户安装的独立 OpenClaw 自动替换龙枢内置版本。

## 总体架构

```text
LongHub Desktop
  ├─ Electron Main
  │    ├─ 设备注册、授权码激活与运行配置获取
  │    ├─ Agent Pack 安装、激活与 Agent Profile 注册
  │    ├─ OpenClaw Gateway 生命周期与独立状态目录
  │    ├─ @longhub/openclaw-compat 版本化兼容契约
  │    └─ 安全隔离的 BrowserWindow
  ├─ OpenClaw Control UI ── 原生 Agent Selector / 独立会话
  ├─ LongHub Tool Bridge ── Agent/Session 上下文与权限收敛
  └─ LongHub Core / Skill Worker
                │
                │ TLS + 设备凭据
                ▼
LongHub Cloud API
  ├─ 设备、租户、授权与能力包
  ├─ 客户端运行配置与功能开关
  ├─ OpenAI 兼容模型代理
  ├─ 配额、审计与用量
  └─ 发布、灰度与回滚策略
                │
                ▼
        管理员配置的模型服务
```

组件设计分别记录在：

- [桌面端设计](apps/longhub-desktop/DESIGN.md)
- [云端模型网关设计](apps/longhub-cloud-api/DESIGN.md)
- [V2 后续开发执行计划](EXECUTION_PLAN_V2.md)
- [历史路线与完成记录](ROADMAP.md)
- [一键智能体切换设计](AGENT_PLATFORM.md)
- [普通用户功能开放策略](PRODUCT_FEATURE_POLICY.md)
- [Skill 开放与生态设计](SKILL_PLATFORM.md)

## 核心边界

### 客户端与 OpenClaw

龙枢安装包固定携带一个经过验证的 OpenClaw 精确版本和对应 Node 运行时。上游发布新版不会
自动改变已安装客户端；升级只能通过新的龙枢版本完成。

内置实例拥有独立的 `OPENCLAW_STATE_DIR`、`OPENCLAW_CONFIG_PATH`、workspace、Gateway
Token 和回环端口。不得读取、复制、迁移或删除用户 `~/.openclaw` 下的数据。

### 客户端与模型

客户端只认识 `longhub/longhub-default`。真实模型地址、Key 和模型 ID 只存在于云端，模型代理
必须再次覆盖客户端提交的 model，不能把 UI 隐藏当作安全边界。

### 设备注册与首次激活

设备注册只建立可轮换的设备身份，不代表设备获得产品使用权。未激活设备只允许访问激活状态和
授权码核销接口；Desktop 必须在准备模型配置、Core、Gateway 和主 WebUI 之前完成云端激活检查。
全新设备显示独立最小权限窗口，成功后才进入原生聊天；已激活设备以后直接进入。

授权码使用 128-bit 加密安全随机值，数据库只保存规范化值的 SHA-256，明文仅在管理员创建响应中
返回一次。授权码可以设置租户、有效期、最大设备数和附带 Pack；核销由存储层原子完成。撤销或
到期后，Cloud API 每次访问模型与受保护产品接口都会重新判定并立即拒绝，不能依赖客户端隐藏
页面。已打开的本机 Gateway 不作为授权边界；它的模型调用会被云端拒绝，激活页在下次启动出现。

### 智能体、能力包与会话

Agent Pack 是签名、授权、安装和回滚的分发单位；Agent Profile 是面向用户的一键切换单位；
Capability/Skill 是智能体可以调用的能力。三者不得混为同一个概念。

龙枢把已安装且已授权的 Agent Profile 编译为 OpenClaw `agents.list`。每个 agent 使用独立
workspace、agentDir 和 session store，并复用后台统一控制的模型别名。Control UI 原生 Agent
Selector 负责切换；龙枢不在当前会话中热替换身份或提示词，切换后创建或恢复目标 agent 的
独立会话，避免上下文和记忆串用。

OpenClaw 工具白名单只控制“是否展示给模型”；真正的执行权限由 LongHub Core 按
`智能体声明 ∩ Pack 授权 ∩ 租户策略 ∩ 用户确认 ∩ 任务上下文` 再次校验。详细设计见
[AGENT_PLATFORM.md](AGENT_PLATFORM.md)。

### Skill 开放边界

普通用户可以自行安装官方签名 Skill、创建或导入无代码内容 Skill，并用受限工作流组合自己
已经获准使用的官方能力。Skill 默认只绑定用户明确选择的 Agent；安装、UI 可见和实际执行是
三个独立状态，任何 Skill 都不能因安装成功而自动取得文件、网络、凭据或企业权限。

企业连接器需要租户管理员配置，写入、发送、删除和企业数据变更需要逐次人工确认。第三方可执行
代码优先在隔离 Cloud Executor 运行；任意 JS/Python/PowerShell、npm/GitHub 包、EXE/DLL、
OpenClaw 插件和 MCP 服务不进入普通用户直接安装范围。详细类型、流程和阶段见
[SKILL_PLATFORM.md](SKILL_PLATFORM.md)。

### 普通用户功能开放边界

龙枢开放完成任务、管理个人内容和理解产品状态所需的功能，但不恢复 OpenClaw 原生管理控制台。
文本聊天、会话管理、本机搜索/导出、当前 Agent 偏好与记忆控制、官方 Agent/Skill 目录、额度、更新、
隐私和诊断可以默认开放。文件、图片、语音、知识库、只读网页搜索、无代码智能体与跨 Agent 摘要转交
按租户策略开放；企业渠道、写操作、定时任务和多 Agent 编排需要管理员授权。

模型、Provider、API Key、Gateway、Token、原始配置、MCP、插件、终端、shell、elevated、原生
Channels 和基础设施面板继续禁止普通用户访问。详细矩阵与 `Feature Policy V2` 见
[PRODUCT_FEATURE_POLICY.md](PRODUCT_FEATURE_POLICY.md)。

### 产品化与上游同步

产品化适配遵循以下优先顺序：

1. OpenClaw 官方配置或扩展点。
2. 稳定的语义属性、主题变量和公开路由。
3. 与上游文件哈希绑定的最小补丁。
4. DOM/CSS 选择器注入只作为受测试的临时兼容手段。

每个适配必须归属于明确的 OpenClaw 版本，不允许存在无版本约束的“通用补丁”。
当前 `@longhub/openclaw-compat` 把精确版本、路由、Selector/Stop/模型控件语义、Gateway RPC、
`replacePaths` 和固定 UI 基线集中为单一事实来源；Desktop 启动时从实际 `openclaw.mjs` 同目录
回读版本，缺失、损坏或漂移时进入可诊断错误页，不启动未知 Gateway。

品牌与中文化由同一契约登记的 UI chrome 薄层完成：只允许修改品牌节点、会话标题、运行状态、
欢迎建议和固定属性，不遍历聊天正文。普通用户窗口只允许停留在 `/chat`，模型、管理导航、命令
搜索和基础设施入口同时由 CSS 与 Main 路由白名单收口。内部 `agent:` key 仍作为路由标识保留，
但不会出现在正文或悬停标题。

## 数据与安全原则

- 设备 Token 使用当前 Windows 用户的 Credential Manager；本地 `device.json` 只保存非敏感设备
  元数据。旧明文凭据必须在写入并回读一致后才删除，失败时保留原文并安全停止。
- Gateway Token 每次启动随机生成，只通过进程环境和 URL fragment 一次性传递。
- 上游模型 Key 使用服务端主密钥加密，管理接口不回显明文。
- 日志、诊断包和遥测不得包含 Token、API Key、完整提示词或用户文件内容。自动遥测只接受严格版本、
  粗粒度桶和固定枚举，客户端不落盘，Cloud 不保存逐设备事件或任何身份列，只保留 UTC 小时聚合。
- Desktop 日志按固定大小和数量轮转；本机清理只处理龙枢 `userData` 白名单内可再生制品。OpenClaw
  会话、记忆、workspace、Pack、凭据和回滚证据只计入 8 GiB 状态上限，不自动删除；超限固定失败。
- 主机从休眠恢复后不信任旧 PID 或套接字，重新验证真实 `/chat` 后才恢复界面；磁盘至少保留
  256 MiB，`ENOSPC/EDQUOT` 统一进入固定存储错误，不回退为模型或 Gateway 配置错误。
- 日志、审计和数据库不得保存授权码明文；审计只记录授权码 ID、尾号和核销结果。
- OpenClaw 状态升级前创建龙枢专属临时快照；健康检查成功后按保留策略清理。
- 服务端至少保留当前稳定版和上一稳定版发布物，用于快速回滚；临时上传和部署备份必须清理。
- Windows 正式发布物必须通过可信 Authenticode 证书签名并带时间戳；签名私钥和密码不得进入仓库、
  日志或普通构建产物。内部未签名候选与正式发布命令必须分离。
- 安装包只能使用品牌清单中已审批且摘要匹配的 SVG/PNG/ICO；临时图标不能通过正式发布门禁。

### Agent Pack 供应链边界

Agent Profile 和 Pack Manifest 都属于不可信发布输入。云端发布与桌面安装必须使用同一个严格
Schema，拒绝未知字段、危险路径、任意插件/可执行入口、真实模型配置和共享记忆。Profile ID、
版本、capability、permissions 与引用文件必须和 Manifest 联合校验。

SHA-256 摘要覆盖排除自引用 `integrity.digest` 后的 Manifest 与全部 Pack 文件，再使用 Ed25519
签名；因此版本、最低客户端版本、权限、Profile 和 workspace 模板都不能在签名后单独修改。
已知限制是签名只能证明发布来源和内容完整性，不能证明业务逻辑安全；未来第三方 Pack 仍需要
审核、静态扫描、租户策略与 Core 执行时权限交集。

## 关键设计决策

| 日期 | 决策 | 理由 | 影响 |
|---|---|---|---|
| 2026-07-28 | 主界面直接使用 OpenClaw Control UI | 保留完整上游能力，避免维护两套聊天界面 | 品牌改造必须保持薄层 |
| 2026-07-29 | 模型由云端统一代理和强制覆盖 | 降低用户配置成本并保护真实 Key | 云端成为模型可用性的关键依赖 |
| 2026-07-29 | 内置 OpenClaw 完全隔离 | 允许与用户原有实例安全共存 | 状态、配置、workspace、端口均需独立 |
| 2026-07-29 | OpenClaw 精确锁版、受控升级 | 防止上游变化直接破坏产品客户端 | 每次升级必须通过兼容门禁 |
| 2026-07-29 | 兼容契约集中到独立 workspace 模块 | 阻止 DOM、路由与 RPC 依赖分散后静默漂移 | 契约变化必须更新摘要并通过真实 UI/Gateway 回归 |
| 2026-07-29 | 用版本化 UI chrome 薄层完成龙枢产品化 | 保留原生 WebUI 且避免长期 fork | 禁止改写聊天正文，所有语义必须进入兼容契约与 E2E |
| 2026-07-29 | 以 OpenClaw 原生多 Agent 作为一键切换底座 | 上游已有隔离 workspace、会话和 Agent Selector | Agent Pack 需编译为 `agents.list`，权限仍由 Core 收敛 |
| 2026-07-29 | 首阶段采用设备授权码门禁 | 无需普通用户账号即可控制安装使用权，且不暴露模型配置 | 注册与授权分离，所有产品 API 在服务端复验激活状态 |
| 2026-07-29 | 正式发布采用签名与品牌双门禁 | 防止未签名程序和临时图标误上线 | 内部/正式构建分离，正式包必须验主体、时间戳和资产摘要 |
| 2026-07-30 | 自动遥测采用严格匿名小时聚合 | 未采集的数据比事后脱敏自由日志更可靠 | Bearer 只用于鉴权限流，存储无身份与原始载荷 |
| 2026-07-30 | Skill 分级开放，不开放任意代码 | 用户可定制能力不应扩大本地主机信任边界 | 先开放官方签名与无代码 Skill，第三方代码优先云端隔离 |
| 2026-07-30 | 用户功能分级开放，不恢复底层控制台 | 全部隐藏会损失产品价值，原样开放会暴露基础设施 | 低风险默认开放，内容/费用按租户，副作用需管理员授权 |

## 变更流程

所有后续工作以 [EXECUTION_PLAN_V2.md](EXECUTION_PLAN_V2.md) 为唯一进度表；[ROADMAP.md](ROADMAP.md)
只保留历史完成记录。设计级变更先更新本文件或对应模块
DESIGN，再实现代码；完成项必须附测试、构建产物或线上验证证据，不能只勾选状态。

## 变更历史

### 2026-07-30 - V2 成为单一后续执行计划

**变更内容**：将未来版本、任务顺序、状态和验收统一迁入 `EXECUTION_PLAN_V2.md`，旧 ROADMAP 改为
历史完成记录。

**变更理由**：功能策略、Skill 路线与旧任务表存在顺序冲突，需要消除多份可执行计划。

**影响范围**：后续版本管理、Desktop/Cloud/Core 开发顺序、发布门禁与进度维护。

**决策依据**：单一进度源可以避免 Feature Policy、确认中心和 UI 扩展前置条件再次被跨过。

### 2026-07-30 - 建立普通用户功能开放矩阵

**变更内容**：新增会话、文件、多模态、知识、Agent/Skill、联网、自动化、企业渠道、设置和隐私的
四级开放策略，并提出统一 `Feature Policy V2`。

**变更理由**：产品不应只剩聊天输入框，也不能恢复包含模型、凭据和基础设施的 OpenClaw 管理面。

**影响范围**：runtime-config、Cloud API、Desktop/OpenClaw UI、Core、知识、Connector、自动化与隐私。

**决策依据**：优先开放本地、只读、用户主动、可撤销功能；按数据、费用、外部副作用和代码风险逐级治理。

### 2026-07-30 - 建立 Skill 分级开放边界

**变更内容**：新增 Skill 开放与生态设计，允许官方签名 Skill 自助安装、用户无代码 Skill 和
受限工作流，明确企业连接器、第三方云端代码、本地 WASM 与禁止项的分阶段边界。

**变更理由**：用户需要按 Agent 自行扩展能力，但现有 Worker 仍是静态可信代码；必须区分内容安装、
能力授权和代码执行，避免绕过 Core、Agent 隔离与客户端主机安全。

**影响范围**：Skill/Pack 契约、Desktop UI 与 Registry、Core/Worker、Cloud Catalog/Executor、
权限、审核、计量和 OpenClaw 兼容门禁。

**决策依据**：复用现有签名、Tool Bridge、在线授权和云端执行边界，按风险从声明式能力逐级开放。

### 2026-07-30 - 建立最小匿名运行指标边界

**变更内容**：增加共享严格遥测 v1、Desktop 有界内存批次、已激活设备 Cloud 入口、每设备限流和
PostgreSQL 小时聚合；同步 OpenAPI、隐私回归和验收记录。

**变更理由**：版本分布、启动与更新健康需要最低限度运行信号，但上传日志、诊断包或自由字段会把
聊天、路径、身份和未知秘密带出本机。

**影响范围**：Observability 共享契约、Desktop Main 生命周期、Cloud API/Store/migration、OpenAPI。

**决策依据**：严格枚举加服务端重解析，从结构上禁止敏感字段；只保留无身份小时聚合，失败不影响产品。

### 2026-07-29 - 建立 Windows 签名与品牌发布门禁

**变更内容**：拆分内部候选和正式发布命令，增加品牌审批清单、资源摘要、内置 Node、安装包资源、
Authenticode 主体及可信时间戳校验。

**变更理由**：当前构建机没有龙枢代码签名证书，现有图标也是临时生成；不能让未签名候选因命令
相同而被误当正式版本上传。

**影响范围**：Desktop 构建脚本、品牌资产、安装包验证、CI 密钥配置和发布文档。

**决策依据**：私钥留在受控构建环境，仓库只记录非秘密的预期签名主体和已审批资产摘要；最终
产物必须使用 Windows 原生 Authenticode 重新验签。

### 2026-07-29 - 增加首次授权码激活门禁

**变更内容**：新增授权码创建、核销、撤销、设备绑定和附带 Pack 授权；Desktop 在任何模型或
Gateway 初始化前显示独立激活页，已激活设备后续直接进入。

**变更理由**：设备匿名注册只解决客户端身份，不能替代商业授权；普通用户也不应接触账号、模型
或 Gateway 配置。

**影响范围**：Cloud API、PostgreSQL 迁移、管理后台、Desktop 启动顺序、OpenAPI 和安全测试。

**决策依据**：服务端逐请求复验关闭 UI 绕过和撤销窗口；授权码摘要存储降低数据库泄露后的风险。

### 2026-07-29 - 完成龙枢 UI 产品化

**变更内容**：统一窗口、页面品牌、临时图标、中文状态、欢迎建议、输入提示和默认会话标题；隐藏
管理导航并把页面导航收口到产品聊天路由，错误页不再向普通用户暴露底层基础设施名词。

**变更理由**：普通用户应感知到龙枢和可切换智能体，而不是需要维护的底层控制面；同时继续复用
原生聊天和多 Agent 能力，避免形成完整 WebUI 分叉。

**影响范围**：兼容契约摘要、Desktop 页面注入、导航白名单、窗口/安装图标、UI E2E 与发布门禁。

### 2026-07-29 - 建立 OpenClaw 版本化兼容层

**变更内容**：新增 `@longhub/openclaw-compat`，集中管理 OpenClaw 精确版本、产品路由、DOM 语义、
Gateway RPC 与固定 UI 基线；Desktop 启动改为核对实际内置版本。

**变更理由**：继续直接复用原生 WebUI 的前提下，需要把不可避免的上游依赖变成显式、可审查、
可回归的发布契约，避免升级后模型入口或 Agent 切换静默失效。

**影响范围**：Desktop 启动、导航策略、Selector、Gateway 热更新、UI 合约与 OpenClaw 升级门禁。

### 2026-07-29 - 建立总体设计基线

**变更内容**：明确产品定位、系统边界、OpenClaw 受控升级策略以及数据安全原则。

**变更理由**：后续开发需要共享的决策基线，避免产品化、上游同步和后台能力各自演进。

### 2026-07-29 - 一键智能体切换成为产品主线

**变更内容**：明确 Agent Pack、Agent Profile、Capability 和会话的边界，采用 OpenClaw 原生
多 Agent 与 Agent Selector 承载切换体验。

**变更理由**：仓库已有套装分发与执行底座，上游也已提供隔离多 Agent；应优先补齐两者之间的
激活和权限桥接，而不是在远期重新建设智能体平台。

**影响范围**：Pack 契约、桌面端配置生成、WebUI、Core 权限、会话和后台授权。

**决策依据**：复用现有能力可以保持原生 WebUI、减少上游分叉，并从架构上避免身份、记忆和
工具权限在智能体切换时串用。
