# LongHub 普通用户功能开放策略

> 状态：产品与开发执行基线  
> 适用基线：LongHub Desktop 0.4.1 / OpenClaw 2026.7.1-2  
> 更新日期：2026-07-30

本文件定义“哪些功能可以开放”和安全边界，不再承担版本排序；实际实施顺序以
[EXECUTION_PLAN_V2.md](EXECUTION_PLAN_V2.md) 为唯一依据。

## 一、目标与总原则

龙枢不应把 OpenClaw 的所有管理入口全部隐藏后只留下一个输入框，也不应把底层控制台原样交给普通
用户。正确边界是：开放能直接完成任务、管理个人内容和理解产品状态的功能；模型、运行时、凭据、
插件和企业策略继续由后台管理。

所有功能使用四级开放状态：

1. **默认开放**：低风险、任务必需，不要求企业额外配置。
2. **租户可控**：有数据、费用或合规影响，由后台按租户/套餐/设备开启，普通用户只使用不配置底层。
3. **管理员授权**：涉及企业连接器、对外发送、自动执行或多人共享，管理员先配置，用户再明确操作。
4. **暂不开放**：会扩大主机、凭据或基础设施控制面，当前不能用 UI 隐藏代替安全隔离。

后台开关、用户入口和服务端鉴权必须使用同一个稳定 `feature_id`。入口可见不代表获得权限；直接调用
API、旧页面或历史会话仍必须执行相同的授权、额度和数据范围检查。

## 二、功能开放总表

### 2.1 对话与会话

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 文本聊天、流式回复、停止生成 | 默认开放 | 保持 OpenClaw 原生能力和真实 E2E |
| 复制回答、重试、继续生成 | 默认开放 | 重试产生新执行并重新计量，不能复用敏感确认 |
| 新建会话、切换 Agent、查看本机历史 | 默认开放 | 会话永久绑定 Agent，不热改身份 |
| 会话重命名、归档、置顶 | 默认开放 | 只改本机索引，不改原始消息或 Agent 归属 |
| 本机会话搜索 | 默认开放 | 索引留在本机，不进入遥测或云端搜索接口 |
| 删除会话与清空当前 Agent 记忆 | 默认开放 | 二次确认、先进入可恢复区、明确备份/回收保留期 |
| 导出单个会话为 Markdown/JSON/PDF | 默认开放 | 用户主动选择保存位置；显示可能包含企业内容的提示 |
| 公开分享链接 | 管理员授权 | 需要身份、ACL、到期、撤回、水印/下载策略和审计；默认关闭 |
| 会话云同步 | 暂不开放 | 设备授权码不等于用户身份；满足加密、冲突、删除和保留证明后再开 |

### 2.2 文件、图片与语音

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 当前会话附件上传 | 租户可控 | 类型/大小/数量限制、恶意文件解析沙箱、明确保留期，不暴露原路径 |
| 图片/截图理解 | 租户可控 | 后台选择支持视觉的模型，用户仍不选模型；删除与内容安全策略一致 |
| 用户主动截图或选区 | 租户可控 | 每次 OS 权限与用户动作，不允许后台持续录屏 |
| 文件另存为/生成文档 | 默认开放 | 使用原生保存对话框和一次性目标句柄；覆盖需确认 |
| 个人文档知识库 | 租户可控 | 用户范围隔离、加密、索引沙箱、删除证明、容量与费用限制 |
| 语音输入/转写 | 租户可控 | 首次麦克风授权，明确本地/云端处理方与保留期，无后台录音 |
| 朗读回答/TTS | 租户可控 | 后台统一提供方与费用，默认不保存音频 |
| 摄像头持续访问、后台录音/录屏 | 暂不开放 | 不允许 Skill 或 Agent 常驻采集 |
| 自动读取剪贴板 | 暂不开放 | 只允许用户点击粘贴，不后台轮询剪贴板 |

当前 runtime-config 已有 `file_upload` 字段，但 Desktop 只严格解析，尚未把它变成附件 UI 与请求链的
完整安全开关。完成文件解析、大小、类型、保留、路由和 E2E 前，不能把该字段标记为已真正开放。

### 2.3 知识、记忆与个性化

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 企业知识引用与来源查看 | 租户可控 | 只检索当前租户/Agent 授权范围，回答显示来源与更新时间 |
| 查看、编辑、删除当前 Agent 的用户记忆 | 默认开放 | 只操作用户覆盖层，不修改签名 Profile；不能跨 Agent 浏览 |
| 助手称呼、语气、头像偏好 | 默认开放 | 存在 Agent 用户覆盖层；不能改发布方、权限、工具或模型 |
| 快捷任务/提示模板 | 默认开放 | 后台可下发，用户可收藏/排序；模板不是授权来源 |
| 用户创建无代码智能体 | 租户可控 | 只能配置名称、说明、知识和已授权 Skill；无模型/插件/代码入口 |
| 复制/派生官方智能体 | 租户可控 | 派生为用户来源 Profile，权限默认不高于原 Agent 且分别授权 |
| 跨 Agent 共享原始记忆或 transcript | 暂不开放 | 默认隔离；只允许用户确认后的最小摘要转交 |

### 2.4 Agent、Skill 与能力组合

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 浏览、搜索、安装和切换官方 Agent | 默认开放 | Catalog + entitlement + Pack 验签 + 兼容检查 |
| 启停、升级、回滚和卸载 Agent | 默认开放 | 历史/记忆默认不静默删除；撤销先阻断执行 |
| 安装官方签名 Skill | 默认开放 | 绑定指定 Agent，安装不等于授权执行 |
| 创建/导入 Content 与 Workflow Skill | 租户可控 | 无任意代码，工作流只能组合已授权 Skill |
| 跨 Agent“带摘要转交” | 租户可控 | 用户预览并确认摘要和目标 Agent，不复制原始记忆/权限 |
| 自动 Agent 路由 | 管理员授权 | 用户可见路由结果、可取消，受 Agent allowlist 与预算限制 |
| 多 Agent 任务编排 | 管理员授权 | DAG 深度/并发/费用硬上限，最小上下文共享，写冲突需确认 |
| Agent 自主创建子 Agent | 暂不开放 | 当前继续关闭 subagent、agent-to-agent 和跨会话工具 |

Skill 的详细类型、供应链和执行边界见 [SKILL_PLATFORM.md](SKILL_PLATFORM.md)。

### 2.5 联网、搜索与内容生成

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 只读网页搜索并显示引用 | 租户可控 | 后台统一搜索提供方、域名策略、超时/大小限制和引用，不暴露 Key |
| 用户提交 URL 摘要 | 租户可控 | 服务端安全抓取，阻断私网/本机/重定向绕过和危险文件下载 |
| 图片生成/编辑 | 租户可控 | 后台选模型、内容安全、版权提示、费用额度和来源标记 |
| 代码解释与代码块生成 | 默认开放 | 只生成文本，不自动执行；下载文件走用户保存确认 |
| 浏览器只读研究模式 | 管理员授权 | 隔离云端浏览器、无用户登录态、域名 allowlist、可见操作记录 |
| 使用用户已登录浏览器自动操作 | 暂不开放 | 登录态、支付、消息和企业系统副作用风险过高 |
| 无限制网络访问/任意下载 | 暂不开放 | 所有网络必须经过 Search、Connector 或隔离 Browser Broker |

### 2.6 任务、提醒与自动化

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 长任务进度、取消、失败重试 | 默认开放 | 幂等、超时、预算和可恢复状态 |
| 本地提醒 | 租户可控 | 只发本机通知，不自动执行企业操作 |
| 定时生成只读摘要/报告 | 管理员授权 | 明确时区、频率、数据范围、费用、停用和审计 |
| 定时对外发送或写企业系统 | 管理员授权 | 管理员允许 + 用户预授权范围；高风险仍需执行时确认 |
| 入站 Webhook 触发任务 | 管理员授权 | 签名、重放保护、来源 allowlist、租户路由和速率限制 |
| Agent 自行创建无限循环/后台常驻任务 | 暂不开放 | 所有任务有期限、预算、所有者和一键停止 |
| 自动付款、删除、授权变更 | 暂不开放 | 即使未来开放也必须逐次强确认，不能靠长期预授权 |

### 2.7 企业渠道与协作

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 飞书/企业微信/Slack/Teams 等企业渠道 | 管理员授权 | 后台配置应用和凭据，用户只选择已授权目标，不见 Token |
| 草拟邮件/消息 | 租户可控 | 生成草稿默认不发送 |
| 发送邮件/消息 | 管理员授权 | 显示接收方和正文摘要并确认，写审计与幂等键 |
| 组织内分享 Agent/Skill/工作流 | 管理员授权 | 发布前扫描/审核、版本、ACL、撤回和来源展示 |
| 多人共同编辑会话 | 暂不开放 | 需要身份、并发冲突、审计、删除和数据保留设计 |
| 普通用户配置原生 OpenClaw Channels | 暂不开放 | 继续隐藏原生渠道控制台，统一走龙枢后台 Connector |

### 2.8 产品设置、隐私与运维

| 功能 | 建议状态 | 开放边界 |
|---|---|---|
| 语言、主题、字号、快捷键 | 默认开放 | 独立龙枢用户设置，不进入 OpenClaw 基础设施配置 |
| 通知、下载、存储用量和数据保留提示 | 默认开放 | 后台策略可向下限制；不允许任意移动受管状态目录 |
| 查看套餐额度、当期用量和到期时间 | 默认开放 | 展示逻辑额度，不显示真实模型、成本密钥或内部路由 |
| 检查更新、查看版本与发布说明 | 默认开放 | 稳定渠道签名更新；灰度/暂停仍由后台控制 |
| 导出脱敏诊断 | 默认开放 | 保持现有严格白名单，不扩展为日志/聊天打包 |
| 导出/删除自己的产品数据 | 租户可控 | 需要明确身份范围、异步状态、备份保留说明和审计 |
| Beta 渠道选择 | 管理员授权 | 企业管理员决定设备组，普通用户不能绕过暂停或最低版本 |
| 模型、Provider、API Key、Gateway、Token | 暂不开放 | 永远由后台统一配置和强制覆盖 |
| 原始配置、系统健康、基础设施、日志控制台 | 暂不开放 | 用户只看产品化状态、公开错误码和脱敏诊断 |

## 三、低风险开放候选

以下功能价值高、对现有安全边界改动小，在 V2 计划排到并完成 UI/回归后可以默认开放：

1. 会话重命名、归档、置顶、本机搜索和单会话导出。
2. 当前 Agent 的用户偏好与记忆查看/编辑/删除。
3. 快捷任务收藏、排序和用户自己的纯文本提示模板。
4. 稳定版检查更新、版本说明、额度/到期展示和存储用量提示。
5. 文件另存为和显式文件选择器，不提供任意路径工具。
6. 官方 Agent/Skill 的目录、安装、启停、升级和卸载。

以下属于租户可控候选，不代表进入 1.0 范围；实际版本以 V2 计划为准：

1. 会话附件、图片理解和企业知识引用。
2. 只读网页搜索、URL 摘要、图片生成和语音输入/TTS。
3. 用户无代码智能体、Content Skill 和受限 Workflow Skill。
4. 跨 Agent 带摘要转交。

## 四、统一 Feature Policy V2

当前 runtime-config 只有 `agent_catalog`、`file_upload`、`tool_execution` 三个布尔值，无法表达用户功能
开放所需的风险、角色和限制。下一版使用严格、版本化的 `longhub/feature-policy/v2`：

```text
feature_id
enabled
scope: global / tenant / plan / device / agent
audience: user / tenant_admin / platform_admin
mode: default / tenant_controlled / admin_approved
limits: count / bytes / duration / concurrency / cost
data_policy: processing_location / retention / export / deletion
required_entitlements
required_permissions
min_desktop_version / max_desktop_version
policy_version / expires_at / emergency_disabled
```

规则：

- 设备 > 套餐 > 租户 > 全局只用于选择最具体策略，不能把高风险功能的管理员要求降级掉。
- 客户端只接受已知 `feature_id` 和字段；未知高风险功能安全关闭。
- 服务端 API 按同一策略再次鉴权，不能只让 Desktop 隐藏按钮。
- 离线缓存只能维持低风险显示；发送、写入、企业连接器、自动化和第三方执行无法在线复验时拒绝。
- 每次策略变更写管理员审计；紧急关闭先阻断执行，再更新 UI。
- 普通用户设置只能在策略允许范围内收窄，不能扩大租户或平台权限。

建议首批稳定 ID：

```text
chat.core                 session.manage           session.local_search
session.export            file.input               image.input
voice.input               voice.output             knowledge.tenant
knowledge.personal        memory.user_controls     web.search_readonly
url.fetch_readonly        image.generate           agent.catalog
agent.custom_nocode       agent.handoff_summary    skill.catalog
skill.user_content        workflow.user_bounded    automation.reminder
automation.scheduled_read channel.enterprise       data.export_delete
```

## 五、UI 形态

普通用户仍以 `/chat` 为主页面。开放功能以三个轻入口承载，不恢复 OpenClaw 管理侧边栏：

- **智能体**：选择、安装、停用和查看当前 Agent 能力。
- **能力**：官方 Skill、用户无代码 Skill、知识和连接器状态。
- **我的**：会话管理、个人偏好、额度、隐私、存储、更新和诊断。

这些入口属于龙枢产品薄层，不能拥有 Node、通用 preload/IPC 或 Gateway Token。需要本机文件、麦克风、
截图和保存操作时，使用 Main 严格校验的单用途请求和一次性能力句柄。每个入口都纳入精确 OpenClaw
版本、路由、DOM 语义和真实 Electron E2E；不新增一套平行聊天工作台。

## 六、能力成熟度层级（非版本顺序）

### 层级 1：低风险产品能力

- Feature Policy V2 与统一服务端复验。
- 会话管理、本机搜索/导出、用户偏好/记忆控制、额度/更新/隐私设置。
- 官方 Agent/Skill 中心。

### 层级 2：受控内容与多模态

- 安全附件链、图片理解、企业/个人知识、只读网页搜索和 URL 抓取。
- 语音输入/TTS、图片生成、用户无代码智能体和 Workflow。
- 跨 Agent 带摘要转交。

### 层级 3：企业连接与自动化

- 管理型 Connector、企业渠道、草稿/发送确认、定时只读任务和 Webhook。
- 第三方云端 Skill、多 Agent 可见编排、组织内审核分享。

### 暂缓

- 会话云同步、公开分享、多方实时协作、登录态浏览器自动化、本地第三方可执行代码。
- 原生 OpenClaw 设置、Channels、MCP、插件、终端、shell、elevated 和基础设施面板。

## 七、验收门禁

每项开放功能至少验证：

- 关闭策略下 UI、直接路由和 API 均不可用；旧会话不能绕过。
- 不出现模型、Provider、Gateway、Token、真实服务地址和企业长期凭据。
- Agent、租户、用户与设备数据不串用；跨 Agent 转交只包含用户确认的摘要。
- 文件/URL/连接器抵御路径穿越、SSRF、重定向绕过、超大内容、恶意解析和秘密回显。
- 写操作、发送、删除和外部副作用确认绑定目标、参数摘要、Agent、Session 和 ToolCall，不能重放。
- 功能开关、授权撤销和紧急停用先阻断执行，且 30 秒内反映到 UI。
- 用量、费用、保留期和执行位置在用户操作前可见。
- OpenClaw 升级后 `/chat`、附件、会话、Agent Selector 和新增入口完成真实 UI/视觉回归。

## 八、关键决策

| 日期 | 决策 | 理由 | 影响 |
|---|---|---|---|
| 2026-07-30 | 开放用户功能，不恢复底层控制台 | 用户需要完成任务和管理内容，不需要维护运行时 | 采用龙枢产品薄入口与后台 Feature Policy |
| 2026-07-30 | 会话搜索默认本地 | 避免为了便利隐式上传企业会话 | 云同步继续默认关闭 |
| 2026-07-30 | 文件、搜索、语音和生成按租户开放 | 它们涉及内容处理、供应商和费用 | 后台配置提供方与限制，用户不选模型/Key |
| 2026-07-30 | 跨 Agent 先做摘要转交 | 直接共享记忆会破坏现有隔离保证 | 用户预览确认，目标 Agent 不继承来源权限 |
| 2026-07-30 | 企业渠道统一走 Connector | 原生 Channels 控制台会暴露凭据和基础设施 | 管理员配置，用户只使用获准动作 |
| 2026-07-30 | 自动化先只读后写入 | 无人值守副作用的恢复和审计成本高 | 写入/发送仍需更强授权和确认 |

## 九、变更历史

### 2026-07-30 - 实施顺序迁入 V2 执行计划

**变更内容**：保留完整功能开放矩阵，将 Feature Policy、文件/知识、多模态和 1.x 的实际版本顺序
统一交由 `EXECUTION_PLAN_V2.md` 管理。

**变更理由**：策略文档负责边界，执行文档负责任务，避免两份文档出现不同优先级。

**影响范围**：功能开放、版本规划和进度维护。

### 2026-07-30 - 建立普通用户全功能开放矩阵

**变更内容**：将会话、文件、多模态、知识、记忆、Agent/Skill、联网、自动化、企业渠道、设置和
隐私功能划分为默认开放、租户可控、管理员授权和暂不开放，并定义 Feature Policy V2 与阶段门禁。

**变更理由**：仅讨论 Skill 会遗漏 OpenClaw 与龙枢已有的其他产品能力；全部隐藏会损失用户价值，
原样开放管理控制台又会泄露模型、凭据和基础设施边界。

**影响范围**：runtime-config、Cloud API、Desktop 产品薄层、OpenClaw 兼容契约、文件/知识/连接器、
Agent/Skill、Core 权限、自动化、隐私与 E2E。

**决策依据**：优先开放本地、只读、用户主动和可撤销能力；含企业数据、外部副作用、后台执行或
第三方代码的功能逐级提高授权与隔离要求。
