// Package managerupdate verifies and stages signed LongHub Manager releases.
// It accepts only the longhub-manager product surface and never mutates the
// user's native OpenClaw installation or data.
package managerupdate
