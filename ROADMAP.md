# LongHub 历史路线与完成记录

> 文档状态：历史基线；未来任务已迁移  
> 当前线上稳定版：LongHub Desktop 0.4.1  
> 内置上游：OpenClaw 2026.7.1-2  
> 更新日期：2026-07-30

本文保留 0.3.6—0.6 已完成能力、历史证据和原 1.0 外部阻断记录。2026-07-30 起，所有尚未开始的
未来任务、版本顺序、状态和验收以 [EXECUTION_PLAN_V2.md](EXECUTION_PLAN_V2.md) 为唯一进度源；
本文不再新增未来任务，避免两份路线并行漂移。

## 一、产品目标和衡量标准

龙枢客户端应让普通用户感知到的是“龙枢助手”，而不是需要自行维护的 OpenClaw 控制面。
OpenClaw 是内置运行底座，龙枢负责产品体验、企业控制、安全、发布和服务保障。

首个正式版的核心指标：

| 指标 | 目标 |
|---|---:|
| 全新安装后进入可聊天页面 | 60 秒内；常规启动 15 秒内 |
| 客户端无配置首次聊天成功率 | ≥ 99% |
| 已安装智能体一键切换耗时 | ≤ 3 秒 |
| 智能体身份、会话、记忆和工具串用 | 0 |
| Gateway 非预期退出后的自动恢复 | 60 秒内 |
| 客户端升级成功率 | ≥ 98% |
| 崩溃无感会话占比 | ≥ 99.5% |
| 模型请求成功率（排除上游明确拒绝） | ≥ 99% |
| 严重密钥泄露、安全越权和跨实例数据串用 | 0 |
| OpenClaw 升级回滚恢复时间 | 30 分钟内 |

## 二、当前基线

### 已完成

- [x] 客户端直接进入 OpenClaw 原生 `/chat` WebUI。
- [x] 客户端只暴露 `longhub/longhub-default`，真实模型由后台统一配置。
- [x] 上游 API Key 不进入客户端，服务端强制覆盖真实 model。
- [x] 内置 Gateway 使用独立状态目录、配置路径、Token 和动态回环端口。
- [x] 内置 workspace、身份和记忆与用户 `~/.openclaw` 完全隔离。
- [x] 用户原有 OpenClaw 与龙枢内置实例可同时运行。
- [x] 自动恢复龙枢专属 SQLite 启动迁移锁。
- [x] 隐藏模型选择和主要设置路由。
- [x] Pack 已具备摘要、签名、原子安装、升级与回滚能力。
- [x] 云端已有 catalog、设备 entitlement、签名下载与撤销基础能力。
- [x] Core / Skill Worker / 权限确认已有原型，HR Agent Pack 可作为首个业务样例。
- [x] OpenClaw 原生支持多 Agent、独立 workspace/agentDir/session 和 Agent Selector。
- [x] Agent Registry 与 Profile → `agents.list` Config Composer 已完成契约实现和真实 Schema 校验。
- [x] 已授权但未安装的 HR Agent 可在原生 Selector 一键下载、验签、激活并进入专属会话。
- [x] 全新设备必须使用后台签发的授权码激活；成功前不启动模型、Gateway 或原生 WebUI。
- [x] 0.3.5 已完成本机真实模型验证和线上发布。

### 已知问题

| ID | 问题 | 影响 | 当前处理 |
|---|---|---|---|
| AGENT-01 | Pack 安装后尚未映射到 OpenClaw `agents.list` | 安装的智能体不能一键切换 | 已解决：启动激活与运行时启停 |
| AGENT-02 | Pack 内 `agent.yaml` 尚无结构化 schema 校验 | 身份、能力和禁止字段边界不可靠 | 已由 Agent Profile V1 解决 |
| AGENT-03 | 直接 WebUI 未挂载旧 preload，Core/Worker 未成为 OpenClaw 工具 | 业务智能体只能聊天，不能安全执行能力 | 0.3.6 建 Tool Bridge |
| AGENT-04 | `pack.enable/disable` 只有 RPC 契约草案 | 安装、激活、停用生命周期不闭环 | 已解决：生命周期协调器 |
| AGENT-05 | 当前任务调用方可以传入 `grantedPermissions` | 权限存在被高估风险 | 0.3.6 改由 Core 计算交集 |
| AGENT-06 | entitlement 撤销尚不能立即阻断已打开会话执行 | 授权撤销存在时间窗口 | 已解决：Core 在线复验 + Selector 同步 |
| UX-01 | 左上角仍显示 OpenClaw 品牌 | 产品归属不清晰 | 已解决：龙枢品牌与临时图标薄层 |
| UX-02 | 会话列表可能出现 `agent:main:...` 内部 ID | 普通用户无法理解 | 已解决：可读标题与悬停 key 清理 |
| UX-03 | 存在 Main Session、Done、responding 等英文 | 中文体验不完整 | 已解决：固定 UI chrome 中文化 |
| UX-04 | 欢迎卡包含频道配置、系统健康等管理动作 | 引导不符合终端用户场景 | 已解决：隐藏管理员建议并保留用户任务 |
| COMPAT-01 | 当前部分产品策略依赖 DOM/CSS 选择器 | 上游 UI 改版后可能失效 | 已解决：版本化契约、真实 UI 合约与视觉门禁 |
| SEC-01 | 设备凭据仍保存在 `device.json` | 本机凭据保护不足 | 已解决：Credential Manager 事务迁移 |
| SEC-02 | 激活限流当前为 API 单实例按设备计数 | 多实例下无法覆盖跨节点/IP 攻击 | 上线前补 nginx IP/全局限流 |
| REL-01 | 安装包未签名且当前仍是临时龙字图标 | SmartScreen 与品牌可信度不足 | 发布门禁已完成；待证书与正式视觉稿 |
| OPS-01 | 缺少自动更新、灰度、崩溃与运行指标 | 发布和故障定位依赖人工 | 0.4.0 处理 |
| CLOUD-01 | 默认模型配置目前为全局单例 | 无法按租户、套餐路由 | 0.5.0 处理 |
| CLOUD-02 | 缺少设备级限速、额度和成本统计 | 无法安全扩量和计费 | 0.5.0 处理 |

## 三、版本路线

本节中的 0.5/0.6 是历史开发里程碑标签，不代表曾经发布对应版本的 Desktop；线上客户端仍以 0.4.1
为准。后续真实 Desktop 版本号从 V2 执行计划重新分配，旧标签不再用于发布判断。

### 0.3.6：一键智能体切换纵向 MVP

目标：复用现有 Agent Pack 与 OpenClaw 原生多 Agent，让用户在同一个聊天界面中一键切换
“通用助手”和“HR 助理”，并证明身份、会话、记忆、工具和授权完整隔离。

详细设计以 [AGENT_PLATFORM.md](AGENT_PLATFORM.md) 为准。

#### Agent Profile 与注册

- [x] 冻结 `longhub/agent-profile/v1` schema，覆盖显示信息、workspace 模板、能力、工具、记忆、生命周期与版本兼容策略。
- [x] 把 Manifest、Profile 和引用文件纳入 Pack 摘要、签名、安装校验与兼容性检查。
- [x] 建立 Desktop Agent Registry，持久化 Pack ID、Profile ID、OpenClaw agentId 和版本映射。
- [x] 保留不可被 Pack 覆盖的 `main` 通用助手；启动时将 active HR Pack 注册为 `longhub.agent.hr`。
- [x] 实现 `pack.enable/disable`，明确安装、启用、停用、撤销、升级、回滚和卸载的数据语义。

#### OpenClaw 配置与切换

- [x] 建立确定性的 Profile → `agents.list` Config Composer。
- [x] 为每个 agent 生成独立 workspace、agentDir、session store、skills/tools/sandbox allowlist。
- [x] 使用 `config.patch + baseHash` 原子激活，并对热加载结果回读验证；失败恢复旧 Gateway、Core policy 与 Registry。
- [x] 复用 Control UI 原生 Selector 样式与 `selectAgent()` 路由，切换时恢复最近会话或创建专属主会话。
- [x] 处理生成中/任务执行中的切换：确认停止后再切换，禁止同一会话热替换身份或 agentId。

#### Tool Bridge 与权限

- [x] 用 OpenClaw 公开 plugin SDK 完成 LongHub Tool Bridge 技术验证。
- [x] 只从工具运行上下文获取可信 `agentId/sessionKey/sessionId`，不信任模型自行提交的身份参数。
- [x] 通过本机受限 RPC 调用 Core；Bridge 不直接执行企业操作，也不向 WebUI 暴露通用 IPC。
- [x] Core 按 Profile、已安装 Pack、entitlement、租户/设备策略、用户确认和任务预算计算有效权限交集。
- [x] entitlement 到期或撤销后立即阻断新工具执行，并从 Selector 移除；不能依赖客户端或 Gateway 重启。
- [x] 定义 Selector 30 秒同步、离线执行安全失败和逐次在线复验；确认记录绑定 agent/session/toolCall 与参数摘要。

#### 0.3.6 验收门槛

- [x] `main` 与 `longhub.agent.hr` 同时出现在原生 Agent Selector；运行时启停热加载已通过锁定版 Gateway 验证。
- [x] 两个 agent 的身份、欢迎语、会话、workspace、memory 和工具列表互不串用。
- [x] HR Agent 只能调用签名 Profile、Pack 与有效 entitlement 共同允许的 HR 技能。
- [x] 伪造 agentId 或 permissions 不能越权；敏感业务操作仍需人工确认。
- [x] 撤销 HR entitlement 后，已打开的 HR 会话也不能继续执行能力。
- [x] 已授权但未安装的 HR Agent 能一键下载、验签、激活并进入专属会话。
- [x] Gateway 重启、Pack 升级/回滚和客户端升级后 agent 映射与历史会话仍稳定。
- [x] 契约、单元、集成、真实 Gateway 和一键切换 E2E 全部通过。

### 0.3.7：品牌产品化与 OpenClaw 兼容层

目标：让用户看到完整的龙枢产品，同时把上游 UI 适配集中到可测试、可替换的薄层。

#### 产品体验

- [x] 替换窗口、左上角、欢迎页的龙枢名称、Logo 和临时图标；正式视觉稿仍可同名替换。
- [x] 统一“龙枢助手”的名称、头像、欢迎语和输入框文案。
- [x] 隐藏 `agent:main:*` 等内部会话键，自动显示可读会话标题。
- [x] 完成 Main Session、Done、responding、状态和错误信息中文化。
- [x] 隐藏频道配置、系统健康等管理员快捷卡片，保留普通用户任务。
- [x] 隐藏“更多”、设置/文档、命令搜索、基础设施入口和无授权侧边工具。
- [x] 全新安装打开干净新会话；升级安装保留龙枢自己的历史会话。
- [x] 定义空白、加载、断网、后台未配置、上游限流和服务不可用页面。

#### OpenClaw 兼容层

- [x] 建立 `openclaw-compat` 模块，集中管理版本、配置、路由、选择器和契约摘要。
- [x] 运行时校验内置 OpenClaw 版本，不匹配时显示可诊断错误而不是静默运行。
- [x] 优先采用上游公开配置和语义属性；记录仍依赖 DOM/CSS 的选择器及用途。
- [x] 为品牌、导航、模型隐藏、会话标题和中文化建立 UI 合约测试；当前缺口由 LH-037-02 修复。
- [x] 固定桌面尺寸做容差视觉基线，关键区域变化时阻止发布。
- [ ] 向上游可复用的品牌或产品模式扩展点提出需求，减少长期补丁面积。

#### 首次激活门禁

- [x] 设备注册与产品授权分离；未激活设备只能访问状态查询和授权码核销接口。
- [x] 后台可创建、查看尾号、限制设备数/有效期/Pack 并撤销授权码，明文仅显示一次。
- [x] Desktop 在模型、Core、Gateway 之前显示独立最小权限激活窗口，成功后才进入原生 `/chat`。
- [x] 模型和受保护产品 API 逐请求复验激活状态，授权码撤销或过期立即阻断新请求。
- [x] 真实 Electron E2E 验证错误码、正确核销、IPC 来源校验和无 Node 边界。

#### 0.3.7 验收门槛

- [x] 新用户不看到 OpenClaw、Gateway、Token、Provider、模型 ID 或内部会话 ID。
- [x] 普通用户页面所有核心文案为中文。
- [x] 模型选择和设置入口同时通过 UI 测试与直接路由测试。
- [x] 未激活设备不能进入聊天；已激活设备以后直接进入且无需选择模型。
- [x] 原有 OpenClaw 运行时，龙枢仍能独立启动并完成真实聊天。
- [x] `test`、`typecheck`、`build`、打包 Gateway 冒烟和 UI 快照全部通过。

### 0.4.0：稳定性、安全与可更新

目标：让客户端具备规模化安装、更新、恢复和诊断能力。

#### 客户端安全

- [x] 设备 Token 迁移到 Windows Credential Manager，旧 `device.json` 凭据完成一次性迁移后清除。
- [ ] 安装包和可执行文件使用可信代码签名证书签名。
- [x] 增加日志脱敏测试，禁止输出设备 Token、Gateway Token、API Key 和用户文件内容。
- [x] 启用 ASAR；必须外置的 OpenClaw 和 Node 文件使用可审查的最小生产依赖闭包。
- [x] 对下载清单或更新元数据增加签名验证，不能只依赖 HTTPS 和文件名。

#### 稳定性

- [x] Gateway 退出后由 Supervisor 限频自动重启，连续失败进入安全错误页。
- [x] 运行配置支持指数退避、短期安全缓存和明确过期时间。
- [x] 区分云端不可达、凭据失效、模型未配置、限流和上游故障。
- [x] 增加只包含脱敏状态的“一键导出诊断信息”。
- [x] 定义日志轮转、状态目录磁盘上限和临时文件清理策略。
- [x] 测试强制退出、断网、休眠恢复、系统重启、端口冲突和磁盘不足。

#### 更新与回滚

- [x] 实现龙枢客户端自动更新，支持稳定、灰度和暂停发布。
- [x] 发布前自动保存龙枢专属状态快照，健康检查通过后按策略删除。
- [x] 保留当前稳定版与上一稳定版安装包，并在健康失败时自动回滚。
- [x] 数据迁移必须声明是否支持降级；当前默认 `snapshot_required` 并恢复固定状态快照。
- [x] 更新失败时保留失败状态、恢复旧状态并启动上一稳定版，不重复安装同一坏版本。

#### 可观测性

- [x] 上报匿名客户端版本、启动耗时桶、Gateway 状态、更新结果和固定公开错误码。
- [x] 建立崩溃率、聊天成功率、上游延迟和升级成功率面板。
- [x] 遥测默认最小化，不上传提示词、回答、文件内容或本地路径。

### 0.5.0：后台统一运营

目标：不同企业和设备可以获得不同的助手、模型、能力与发布策略。

#### 运行配置协议

- [x] 为 runtime-config 增加明确的 `schema_version` 和客户端兼容范围。
- [x] 支持 ETag、有效期和服务端紧急禁用开关。
- [x] 后台下发助手名称、头像、欢迎语、快捷任务和功能开关。
- [x] 客户端只接受白名单字段和同源服务地址，配置无效时安全失败。
- [x] 记录配置版本，诊断时能够还原设备当时使用的策略。

#### 模型运营

- [x] 模型配置从全局单例升级为租户/套餐/设备策略。
- [x] 支持主模型、备用模型、超时、重试和熔断策略。
- [x] 增加设备与租户级速率限制、日/月额度和并发限制。
- [x] 统计输入、输出、缓存 Token、成本、错误率和响应时间。
- [x] 支持上游密钥轮换，不中断正在运行的客户端。
- [x] 保持服务端强制 model 覆盖，客户端永远不能越权选模。

#### 设备与发布运营

- [x] 后台展示设备在线状态、客户端版本、最后成功调用和错误摘要。
- [x] 支持设备停用、凭据轮换、版本最低要求和分组灰度。
- [x] 所有模型、助手、能力和发布变更写入管理员审计日志。

### 0.6.0：知识库、能力生态与市场

目标：在已经稳定的一键智能体平台上扩展企业知识和能力生态，不重新套一层平行工作台。

- [x] 建立按租户、用户和设备分发的 Agent Catalog，并支持分类、搜索、版本与兼容信息。
- [x] 接入企业知识库、文件问答和引用来源展示。
- [x] 扩展 Capability/Skill 依赖解析、兼容性测试和组合规则。
- [x] 为第三方 Pack 建立审核、签名、发布、撤回和安全扫描流程。
- [x] 评估会话云同步；先定义加密、租户隔离、删除和保留策略。
- [x] 接入套餐、授权到期、用量计费和管理员报表。

### 后续版本与 1.0 门槛

原 0.7—0.9 计划经复审后废止，不再作为执行依据。新顺序为：

1. 0.5 平台开放基础。
2. 0.6 官方 Skill 中心。
3. 0.7 文件、知识与会话。
4. 0.8 无代码智能体与受限工作流。
5. 0.9 正式商用加固。
6. 1.0 正式商用；媒体、连接器、自动化和第三方生态进入 1.x。

任务、前置条件和门禁统一见 [EXECUTION_PLAN_V2.md](EXECUTION_PLAN_V2.md)。

## 四、OpenClaw 同步与升级规范

### 版本原则

- `package.json` 必须锁定完整 OpenClaw 版本，不使用 `latest`、范围版本或自动漂移锁文件。
- 已安装龙枢不响应用户独立 OpenClaw 的升级；两者进程和数据始终分离。
- 上游升级只能进入新的 LongHub 版本，不在同一龙枢版本中悄悄替换。
- 安全修复可以走快速通道，但不得绕过隔离、启动、模型和回滚测试。

### 兼容矩阵

| LongHub | OpenClaw | 内置 Node | 适配器 | 状态 |
|---|---|---|---|---|
| 0.3.5 | 2026.7.1-2 | 25.9.0 | 当前产品策略 | 稳定基线 |
| 0.3.6 | 2026.7.1-2 | 25.9.0 | 多 Agent 激活/Tool Bridge/一键安装 v1 | 功能验收完成，内部候选 |
| 0.3.7 | 2026.7.1-2（升级前重新评估） | 25.9.0 | UI 兼容层 v1 已建立 | 产品化进行中 |
| 0.4.0 | 2026.7.1-2 | 25.9.0 | Credential Manager 凭据存储与 V1→V2 迁移 | 安全验收完成，内部候选 |
| 0.4.1 | 2026.7.1-2 | 25.9.0 | sandbox 激活 preload CommonJS 热修复 | 激活 E2E 与发布验收完成，内部候选 |

每次升级必须补充矩阵，不允许用“兼容最新版”替代具体版本。

### 标准升级流程

1. 阅读上游发布说明、安全公告、配置变更、状态迁移和 Node 版本要求。
2. 在独立升级分支修改精确版本，生成依赖和许可证差异。
3. 对全新状态和上一稳定版状态各启动一次，验证迁移结果。
4. 运行单元、类型、构建、Gateway、真实模型、路由和 UI 合约测试。
5. 在原有 OpenClaw 已运行、默认端口占用的机器上做共存测试。
6. 对欢迎页、会话页、消息发送、错误页做固定尺寸截图比较。
7. 打包签名候选版，验证安装、升级、卸载、状态快照和回滚。
8. 先内部验证，再按 5% → 25% → 100% 灰度；每阶段观察关键指标。
9. 全量健康后清理临时发布备份，保留上一稳定版正式发布物。
10. 更新兼容矩阵、DESIGN、变更记录和发布说明。

### 升级阻断条件

出现以下任一情况不得发布：

- 工作区、身份、记忆或会话读取到了用户 `~/.openclaw`。
- 模型选择、Gateway、Token、Provider 或设置入口对普通用户重新可见。
- 上游状态迁移后无法可靠恢复上一稳定版。
- UI 合约或关键截图发生未经确认的变化。
- 安装包内 Node 不满足 OpenClaw engines。
- 真实模型请求、流式响应、文件输入或取消请求出现回归。
- 存在未清理的密钥、上传临时文件或部署备份。

## 五、测试与发布门禁

### 每次代码变更

- 单元测试、类型检查和 lint 通过。
- 代码变更同时更新对应测试和设计文档。
- 不在日志、配置、快照和测试夹具中写入真实密钥。

### 每个桌面候选版

- 全新安装与上一稳定版升级测试。
- 内置 Gateway 启动、异常退出恢复和端口冲突测试。
- 独立 state/config/workspace/identity 测试。
- Agent Profile 校验、Profile → `agents.list` 映射和原子激活/回滚测试。
- 多 Agent 身份、会话、记忆、文件和工具权限隔离测试。
- Tool Bridge 可信上下文、Core 权限交集和 entitlement 实时撤销测试。
- 普通用户导航和固定模型策略测试。
- 打包版 Node 与 OpenClaw Gateway 冒烟测试。
- 中文化、品牌、欢迎页、会话标题和错误页 UI 快照。
- 至少一次后台真实模型聊天验证。

### 每次线上发布

- 安装包版本、大小、SHA-256 和签名一致。
- 下载清单原子替换，公网 HEAD 与下载清单一致。
- 服务端先完成兼容 API 部署，再灰度客户端。
- 发布后确认无 `.upload*`、`.deploy-*`、`portal.old`、`admin.old` 等临时备份。
- 记录发布版本、OpenClaw 版本、回滚版本和验证证据。

## 六、历史执行清单

以下任务已作为历史证据保留；未来任务编号和顺序见 V2 执行计划。

| 顺序 | ID | 任务 | 前置条件 | 状态 | 完成证据 |
|---:|---|---|---|---|---|
| 1 | LH-036-01 | 冻结 Agent Profile V1 schema、禁止字段和 Pack 校验 | 现有 Pack Schema | 已完成 | 契约文档、20 个 Pack/HR 测试及分发 E2E |
| 2 | LH-036-02 | 实现 Agent Registry 与 Profile → `agents.list` Config Composer | 01 | 已完成 | 11 个 Registry/Composer 测试及真实 OpenClaw 配置校验 |
| 3 | LH-036-03 | 激活 `main` + HR 双智能体并隔离 workspace/agentDir/session | 02、现有 HR Pack | 已完成 | active Pack 集成测试、真实 Config 校验、双 Agent Gateway 冒烟、Selector DOM 合约 |
| 4 | LH-036-04 | 完成 LongHub Tool Bridge POC | OpenClaw plugin SDK 验证 | 已完成 | 7 个 Bridge/Core 边界测试、真实 Plugin validate/runtime inspect |
| 5 | LH-036-05 | 由 Core 计算有效权限交集并禁止调用方授予权限 | 01、04 | 已完成 | 13 个 Bridge/Core 安全边界测试、真实云端授权撤销复验 |
| 6 | LH-036-06 | 实现 `pack.enable/disable` 和 entitlement 实时停用 | 02、05 | 已完成 | 4 个生命周期事务测试、真实云端撤销、锁定版 config.patch/list 冒烟 |
| 7 | LH-036-07 | 完成原生 Agent Selector 一键切换 E2E | 03—06 | 已完成 | 真实 Gateway + Electron 点击 E2E、隔离报告 |
| 8 | LH-036-08 | 完成 0.3.6 重启、升级、回滚与共存验收 | 03—07 | 已完成 | 双真实 Gateway 共存/重启 E2E、状态连续性测试、0.3.6 候选版验收记录 |
| 9 | LH-036-09 | 已授权未安装 HR 一键下载、验签、激活并进入专属会话 | 06—08 | 已完成 | 全新 userData 云端授权 + 真实 Gateway/Electron E2E |
| 10 | LH-037-01 | 建立版本化 `openclaw-compat` 与 UI 合约测试 | 0.3.6 | 已完成 | 契约摘要、运行时版本预检、真实 UI 合约与容差视觉回归 |
| 11 | LH-037-02 | 完成品牌、中文化、导航和会话标题产品化 | 10、产品素材 | 已完成 | 版本化 UI 薄层、龙枢临时图标、真实 Electron 合约与视觉回归 |
| 12 | LH-038-01 | 建立首次授权码激活门禁 | 设备注册、管理后台 | 已完成 | 云端核销/撤销测试、真实 Electron 激活窗口 E2E、安全设计记录 |
| 13 | LH-040-01 | 采购/配置代码签名证书和正式安装图标 | 外部采购 | 进行中 | 内部/正式构建已隔离并有强制验签；待证书与正式视觉稿 |
| 14 | LH-040-02 | 设备凭据迁移 Credential Manager | 无 | 已完成 | 真实 Win32 凭据读写、事务迁移与失败保留测试 |
| 15 | LH-040-03 | 统一日志与审计脱敏 | 14 | 已完成 | 共享递归脱敏、Desktop 结构化日志、Cloud 审计二次清理与泄露回归 |
| 16 | LH-040-04 | 启用 ASAR 并缩小必须外置的运行时范围 | 15 | 已完成 | 依赖闭包白名单、物理暂存构建、打包态 Core/Worker/Bridge/Gateway 冒烟 |
| 17 | LH-040-05 | 对下载清单和更新元数据增加签名验证 | 16 | 已完成 | 独立 Ed25519 Update key、流式摘要、严格 manifest、sequence 防回滚、历史 key 轮换与受限下载路径 |
| 18 | LH-040-06 | 建立稳定渠道安全自动更新事务 | 17 | 已完成 | 预置信任门禁、原生双确认、摘要与 Authenticode 复验、停机快照、NSIS 启动、跨版本健康标记 |
| 19 | LH-040-07 | 增加签名灰度与暂停发布策略 | 18 | 已完成 | v2 签名 rollout、固定 cohort、策略 sequence、默认暂停、管理操作和安装前撤回复验 |
| 20 | LH-040-08 | 保留上一稳定版并完成二进制自动回滚 | 18、19 | 已完成 | 精确版本制品、双安装器库存、三次/180 秒阈值、幂等状态恢复、坏版本抑制与演练证据 |
| 21 | LH-040-09 | Gateway 运行中故障恢复与产品化错误页 | 15、20 | 已完成 | 真实 `/chat` HTML 健康门槛、连续失败复位、固定错误码、迟到探测隔离和泄漏回归 |
| 22 | LH-040-10 | 运行配置指数退避、安全缓存与明确有效期 | 15、21 | 已完成 | 严格 v1 schema、三次退避、同源同设备十分钟缓存、401/403 禁止回退与过期/损坏回归 |
| 23 | LH-040-11 | 一键导出白名单脱敏诊断信息 | 21、22 | 已完成 | 严格诊断 v1、错误页固定导航、原子安全写入、敏感信息回归与真实 Electron 点击 |
| 24 | LH-040-12 | 日志轮转、状态目录上限与临时文件清理 | 23 | 已完成 | 5×5 MiB JSONL、8 GiB 硬上限、固定命名/年龄白名单、pending 引用与符号链接保护 |
| 25 | LH-040-13 | Windows 主机中断与资源不足稳定性矩阵 | 21—24 | 已完成 | 强退/断网/重启/端口真实证据、resume 复检与受控重启、256 MiB 磁盘门槛 |
| 26 | LH-040-14 | 最小匿名运行指标与遥测边界 | 03、09—13 | 已完成 | 严格 v1 枚举、已激活设备限流、32 项/16 KiB 内存批次、小时聚合、身份与内容拒绝回归 |
| 27 | LH-040-15 | 匿名健康指标与管理看板 | 14 | 已完成 | 严格运行标记、模型 TTFB/结果小时聚合、近 24 小时 Admin 看板与失败不阻断回归 |
| 28 | LH-050-01 | 分层 runtime-config 与产品策略 | 22、27 | 已完成 | ETag、兼容范围、紧急停用、严格产品/功能字段及 Desktop 消费 |
| 29 | LH-050-02 | 多作用域模型路由、韧性、额度与计量 | 28 | 已完成 | device>plan>tenant>global、备用/重试/熔断、速率/日月/并发与成本聚合 |
| 30 | LH-050-03 | 设备运营与全量管理审计 | 29 | 已完成 | 最后在线/模型成功/错误、停用、最低版本、分组、一次性凭据轮换与审计 |
| 31 | LH-060-01 | 可搜索兼容 Agent Catalog 与能力依赖 | 30 | 已完成 | 查询/分类/版本/兼容过滤，dependsOn 缺失/重复/循环拒绝 |
| 32 | LH-060-02 | 租户知识库查询与引用 | 31 | 已完成 | 独立密钥加密、租户 AAD、正文不进列表/审计、后台操作面和引用测试 |
| 33 | LH-060-03 | 第三方 Pack 审核发布与会话同步评估 | 31 | 已完成 | 固定安全扫描、审核后台、完整 HTTP 发布 E2E、审计及默认不启用的同步边界 |
| 34 | LH-060-04 | 套餐用量计费与管理员报表 | 29 | 已完成 | Product/Order/Entitlement 到期链路、日月 Token/成本与估算标记报表 |
| 35 | LH-100-01 | 商用合规与企业数据边界草案 | 34 | 已完成 | 隐私、协议要点、企业数据分类、诊断与会话同步边界 |
| 36 | LH-100-02 | 客服、事件升级、公告与紧急停发 Runbook | 35 | 已完成 | P0—P3、脱敏收集、灰度阈值、停发恢复和复盘流程 |
| 37 | LH-100-03 | 外部商用门槛清单 | 35、36 | 已完成 | 证书/KMS/Windows VM/观察期/恢复演练/法务审批均显式阻断，不伪造完成 |

`main + HR` 的启动、Tool Bridge、Core 权限交集、Pack 运行时启停/撤销、原生 Selector、一键安装、
Gateway 重启、Pack 升级/回滚、客户端状态升级和既有 OpenClaw 共存均已有自动化证据。0.3.6
纵向功能闭环已完成；LH-037-01 已把版本、配置、路由、Selector 和仍依赖 DOM/CSS 的适配集中到
版本化 `openclaw-compat`，并建立真实 UI 合约与容差视觉门禁。LH-037-02 已完成品牌、中文化、
普通用户导航和可读会话标题。LH-038-01 已增加首次授权码门禁，并将注册与产品使用权分离。
LH-040-01 已完成构建与发布门禁，当前等待可信代码签名证书主体和正式视觉稿；材料到位后无需改
应用代码即可生成并验收正式包。LH-040-02 已把设备 Token 迁入 Windows Credential Manager，并
对旧明文文件执行“写入、回读、再清除”的失败安全迁移。LH-040-03 已统一控制台和审计日志出口，
并用固定秘密标记验证设备/Gateway Token、API Key、授权码和用户文件内容不会输出。LH-040-04 已将
Electron 应用代码封装进 ASAR，仅把独立 Node 必需的 OpenClaw/Bridge 生产闭包外置；闭包漂移、
未知外置文件或打包态任一进程链失败都会阻断候选发布。
LH-040-05 已建立独立 Update Ed25519 信任链：Cloud 只发布绑定版本、渠道、平台、摘要和单调序列的
严格签名元数据，Desktop 使用预置信任公钥并拒绝重放/同序列异文，安装前流式核对制品大小与
SHA-256。LH-040-06 已把稳定渠道接入 Electron Main：只有安装包内审批通过的信任清单才启用，下载
和安装使用原生双确认，并在摘要与 Authenticode 验证后停机、快照、启动 NSIS；新版本真实 WebUI
健康后清除 pending。LH-040-07 将更新契约升级为 v2，把 active/paused、灰度基点、固定 cohort seed
和策略时间纳入签名；上传默认暂停，后台每次调整都递增 sequence，Desktop 固定分桶且安装前再次
验签。LH-040-08 进一步要求安装前准备并复验当前版本安装器，使用 pending v2 绑定新旧制品和状态
快照；新版本连续三次启动失败或 180 秒未健康时恢复 LongHub 状态并启动旧安装器，回滚后的坏版本
被本地抑制。当前信任清单仍为 pending，因此不会用测试信任锚误启用，也尚未覆盖线上 0.3.7。
LH-040-09 把 Supervisor 状态接入主窗口：异常退出立即显示自动恢复页，配置错误停止重试，重启耗尽
进入固定安全页；只有同源 `/chat` 返回 2xx HTML 后才回到 OpenClaw 原生界面。错误分类只生成白名单
文案和短错误码，不把上游响应、URL、Token 或本机路径拼进页面；未增加 preload、Node 或 IPC。
LH-040-10 为 runtime-config 增加严格 v1 schema、配置版本和十分钟有效期；Desktop 只对网络、429/5xx
执行三次指数退避，耗尽后仅使用与 Cloud origin、设备 ID 绑定且未过期的公开元数据缓存。401/403、
协议错误、跨设备/Cloud、损坏或过期全部关闭失败；缓存不含 Token 或上游信息，也不替代模型逐请求授权。
LH-040-11 在固定产品错误页增加诊断导出入口；Main 只在错误页可见时接受精确自定义导航，并用原生
对话框保存严格 v1 JSON。报告不扫描日志或状态目录，只含版本、公开错误码、生命周期阶段和数量；
不含设备身份、Token、聊天、URL、端口、PID 或路径，也不会自动上传。LH-040-09 已完成的错误分类同步
在稳定性清单标记完成。
LH-040-12 将同一脱敏 JSONL 落入 5×5 MiB 轮转文件；启动时只清理固定命名且超过 24 小时的临时制品
和超过 7 天的已完成更新下载，并保护 pending 引用。受管状态超过 8 GiB 后进入固定 `LH-ST-001`，
不会按配额删除 OpenClaw 会话、记忆、workspace、Pack、凭据或回滚证据，也不枚举用户 `~/.openclaw`。
LH-040-13 补齐主机故障矩阵：休眠恢复后不信任旧 PID，必须重新验证 `/chat`；失败时由 Supervisor
合并并发重启。磁盘剩余空间低于 256 MiB 或关键写入出现 `ENOSPC/EDQUOT` 时显示固定 `LH-ST-002`。
强退、断网、系统重启和端口冲突复用真实进程、同一 userData、网络故障注入和真实监听端口验证。
LH-040-14 建立 `longhub/client-telemetry/v1`：Desktop 仅在内存中短暂合并版本、启动耗时/Agent 数量桶、
Gateway 枚举状态、更新结果和固定公开错误码，最多 32 项/16 KiB；失败即丢弃且不阻断启动、聊天、更新
或退出。Cloud 只接受已激活设备，Bearer 仅用于每设备进程内限流；事件立即折算为小时聚合，数据库没有
设备、用户、租户、会话或原始载荷字段。提示词、回答、文件、路径、URL、端口、PID、异常和任意标签
在共享严格解析器处拒绝。本项没有增加运营面板；崩溃率、聊天成功率和上游延迟仍需独立隐私设计。
LH-040-15 在不扩大内容采集面的前提下完成健康面板：Desktop 只用本地 `running|clean` 标记推导上一
进程退出结果；Cloud 模型代理自身按上游响应头统计固定结果和 TTFB 桶。Admin 只读取近 24 小时匿名
聚合，展示异常退出率、模型成功率/延迟、升级成功率、固定错误码和版本分布，不提供逐设备事件。

## 七、风险登记

| 风险 | 概率 | 影响 | 缓解措施 | 触发信号 |
|---|---|---|---|---|
| OpenClaw UI 结构变化 | 高 | 高 | 精确锁版、兼容层、UI 合约和截图门禁 | 选择器或路由测试失败 |
| OpenClaw 状态迁移不可逆 | 中 | 高 | 升级快照、迁移演练、保留上一稳定版 | 老状态启动失败 |
| 云端模型代理成为单点 | 中 | 高 | 健康检查、熔断、备用上游和容量监控 | 成功率或延迟恶化 |
| 设备 Token 本地泄露 | 中 | 高 | Credential Manager、轮换和日志脱敏 | 异常设备调用 |
| 授权码被批量试探或跨节点绕过限流 | 中 | 高 | 128-bit 随机码、摘要存储、设备/IP/全局限流 | 激活失败率或单 IP 尝试数异常 |
| UI 隐藏被误当安全控制 | 中 | 高 | 服务端强制授权、选模和额度策略 | 直接 API 可越权 |
| Agent 切换造成身份或记忆串用 | 中 | 严重 | 独立会话/workspace/agentDir、不可改绑、E2E 隔离测试 | 切换后出现其他 agent 内容 |
| Tool Bridge 信任模型提交的身份或权限 | 中 | 严重 | 使用运行时可信上下文，Core 重算权限交集 | 伪造参数可调用未授权技能 |
| entitlement 撤销存在执行窗口 | 中 | 高 | Core 执行前实时校验，客户端停用作为体验层 | 撤销后任务仍可提交 |
| 自动更新破坏可运行版本 | 中 | 高 | 签名、原子安装、灰度和回滚演练 | 更新失败率上升 |
| Update 签名私钥泄露或轮换顺序错误 | 低 | 严重 | 密钥服务、独立用途 key、预置双公钥后再切签名、保留历史 key | 未知 key 拒绝率或异常签名版本 |
| 大规模分发成本失控 | 中 | 高 | 配额、限速、成本统计和套餐策略 | 单设备异常 Token 消耗 |
| 长期维护完整上游分叉 | 低 | 高 | 保持薄层、优先上游扩展点 | 补丁面积持续增长 |
| 控制面核心文件持续膨胀 | 中 | 中 | 新功能优先拆分路由/存储域模块，按测试边界渐进重构 | 单文件超过 500 行或评审困难 |
| 用户安装 Skill 变成任意代码执行 | 中 | 严重 | 分类型 Schema、内容解释器、禁止原生入口、第三方代码优先云端隔离 | 包含脚本/插件/MCP 的包可在本机运行 |
| Skill 跨 Agent 复用权限或记忆 | 中 | 严重 | 显式 Agent Binding、逐调用可信上下文和 Core 在线鉴权 | A Agent 的确认或数据被 B Agent 使用 |
| 第三方 Skill 外传数据或滥用凭据 | 中 | 严重 | 管理员审批、短时凭据代理、出网白名单、隔离执行和撤回 | 未登记域名访问或长期凭据出现在 Skill 环境 |
| 文件/URL 输入导致恶意解析或 SSRF | 中 | 严重 | 文件解析沙箱、类型/大小限制、URL 安全抓取与私网阻断 | 可访问本机/私网或解析器异常消耗资源 |
| 自动化产生无人值守副作用 | 中 | 严重 | 先只读、任务所有者/期限/预算、写入确认、幂等和一键停用 | 定时任务未确认即发送、删除或付款 |
| 功能开关只隐藏 UI 未保护 API | 中 | 高 | Feature Policy V2 与服务端逐请求复验 | 直接请求可使用已关闭功能 |

## 八、进度维护规则

- 状态只使用：`待开始`、`进行中`、`阻塞`、`已完成`。
- 同一任务只能有一个明确负责人；外部依赖必须写在“前置条件”。
- “已完成”必须同时具备实现、测试、文档和验证证据。
- 每个版本开始时冻结范围；新增需求默认进入下一版本，除非属于安全或数据风险。
- 每次 OpenClaw 版本变化都必须更新兼容矩阵和升级记录。
- 每次正式发布后更新当前稳定版、完成项、遗留问题和线上指标。

## 九、待确认的产品决策

以下事项会影响后续设计，需要在对应开发开始前确认：

- 第一阶段是否固定只交付“通用助手 + HR 助理”；本文按此作为默认范围。
- 智能体授权到期后，历史会话采用只读保留、隐藏还是按租户策略删除。
- 正在生成或执行任务时采用“确认停止后切换”；15 秒内未停止则保持原智能体。
- 龙枢最终中文名、英文名、Logo、主色和安装图标。
- 助手默认名称是否固定为“龙枢助手”，还是允许按租户配置。
- 普通用户可以看到哪些 OpenClaw 原生功能和侧边栏入口。
- 首批主推的 4—6 个业务快捷任务。
- 第一阶段已确定使用设备授权码，不要求普通用户登录个人账号；账号体系留给后续跨设备/云同步场景。
- 会话是否只保存在本机，以及未来云同步的合规与保留要求。
- 代码签名证书主体和发布渠道。
- 企业管理员是否默认允许用户创建和导入无代码 Skill；本文按“可由租户关闭”设计。
- 第三方 Skill 的首批合作发布方与允许的企业连接器；在名单确认前不开放公共上传。

## 十、变更记录

### 2026-07-30 - 未来任务迁移到 V2 单一进度源

- 原 0.7—0.9 未来路线经复审废止，Feature Policy、扩展面、确认中心、Skill、文件知识和商用门禁已重排。
- 本文保留历史里程碑与完成证据，不再承担后续任务排序。
- 旧 0.5/0.6 是开发阶段标签，不代表线上曾发布对应 Desktop 版本。

### 2026-07-30 - 增加 Skill 分级开放路线

- 新增 0.7 官方 Skill 中心、用户 Content/Workflow Skill 和 0.8 企业连接器/第三方云端 Skill。
- 明确普通用户可安装不等于可执行；Skill 必须绑定指定 Agent，并由 Core 逐次鉴权。
- 任意脚本、原生程序、OpenClaw 插件、MCP 服务保持禁止，本地 WASM 留到商用后独立评估。
- 将工作拆为 LH-070-01 至 LH-080-04，并补充供应链、跨 Agent 与数据外传风险。

### 2026-07-30 - 扩展普通用户功能开放路线

- 新增 0.7.1 会话/文件/知识/多模态/个性化与 0.9 受管自动化、企业渠道和多 Agent 编排。
- 定义 Feature Policy V2，修正现有 `file_upload` 仅解析但未完整执行的问题。
- 将模型、Provider、Gateway、原始设置、MCP、插件、终端和基础设施面板继续保留在普通用户边界外。
- 会话云同步、公开分享、登录态浏览器自动化和多人实时编辑在身份、加密、删除与审计成熟前保持关闭。

### 2026-07-30 - 完成本地可执行路线与总门禁

- 连续完成 LH-040-15、0.5 后台统一运营、0.6 知识与生态，以及 1.0 可在本地完成的合规/运维材料。
- 知识正文改为独立密钥 AES-256-GCM + tenant AAD；后台补齐知识文档与第三方审核操作面，拒绝 Pack
  不保留危险文件正文；OpenAPI 与完整审核 HTTP E2E 同步完成。
- 全仓 test 27/27、typecheck 29/29、lint 29/29、build 17/17 通过；安全、质量、变更三项技能门禁通过。
- 2026-07-30 已将 Cloud API、Executor、Portal 与 Admin 更新到 `154.9.26.158`，服务端健康检查通过并删除
  本次临时备份；Desktop 正式安装包仍未重打，线上 `0.3.7` 安装包保持不变。
- 未提交或推送；证书/正式 KMS/真实 Windows VM/灰度观察期/恢复演练/法务审批继续作为外部阻塞。

### 2026-07-30 - 完成主机中断与资源不足稳定性测试

- Gateway 强制退出继续按限频策略恢复，真实 `/chat` 健康后才清零失败次数。
- 断网只允许同源、同设备且未过期的十分钟缓存；无安全缓存时固定关闭失败。
- Windows 休眠恢复新增主动复检；PID 消失或页面不健康时合并并发请求并受控重启 Gateway。
- 新进程/新 Store 读取同一 userData，验证系统重启后的 Registry、会话、pending 更新与 Agent 连续性。
- 默认端口由未知服务占用时使用真实回环空闲端口，不连接或终止未知进程。
- 受管状态之外再保留 256 MiB 磁盘余量；`ENOSPC/EDQUOT` 映射固定 `LH-ST-002`，不盲删用户状态。
- 全仓 64 个测试文件、325 项通过，4 项 PostgreSQL 条件跳过；17 个 workspace 的 test、typecheck、
  lint、build 与安全、质量、变更门禁全部通过。

### 2026-07-30 - 完成日志轮转与受管状态维护

- Desktop 脱敏日志按 5 MiB 单文件、5 文件固定轮转，日志目录或目标异常时关闭文件 sink。
- 维护器只处理龙枢 `userData` 白名单内固定命名的过期临时文件、Pack staging 和更新下载。
- pending 更新引用的快照、目标/回滚安装器和失败状态显式保护；符号链接、junction、realpath 越界拒绝。
- 受管状态只统计普通文件大小；8 GiB 后固定显示 `LH-ST-001`，不盲删会话、记忆或其他不可再生状态。
- 清理报告只含计数和字节数；用户独立 `~/.openclaw` 保持完全隔离。
- 全仓 64 个测试文件、318 项通过，4 项 PostgreSQL 条件跳过；17 个 workspace 的 test、typecheck、
  lint、build 与三项代码门禁全部通过。

### 2026-07-30 - 完成 Gateway 运行时恢复与产品化错误页

- Supervisor 在退避前发出明确 restarting 状态，真实 `/chat` 健康后才清零连续失败次数。
- Main 在运行中显示自动恢复、配置错误和重启耗尽安全页，恢复成功后自动返回原生 `/chat`。
- 建立云端不可达、凭据失效、需要激活、模型未配置、限流、上游故障和本机故障的稳定错误分类。
- 错误页只接受枚举，不显示原始响应、服务 URL、Token、本机路径或用户内容；保持 CSP、sandbox、
  无 preload、无 Node、无通用 IPC。
- 全仓 60 个测试文件、271 项通过，4 项 PostgreSQL 条件跳过；17 个 workspace 的类型、Lint、构建和
  安全/质量/变更门禁通过。
- 本轮未部署、未重打候选、未覆盖线上 0.3.7；正式签名、公钥和图标外部阻塞保持不变。

### 2026-07-30 - 完成上一稳定版二进制自动回滚

- 更新 v2 签名载荷加入 `rollback_data_strategy`；当前发布固定为 `snapshot_required`。
- Cloud 新增精确版本签名 metadata 接口，Desktop 安装前必须准备当前版可信安装器。
- 本地库存只保留当前版和上一版；pending v2 同时绑定新旧 metadata、安装器、快照和回滚阶段。
- 目标版本连续三次未健康或 180 秒无真实 WebUI 健康信号时，幂等保留失败状态、恢复快照并启动旧版。
- 旧版成功启动后记录回滚并抑制同一坏版本；更高版本仍可继续更新。
- 本轮未部署、未重打候选、未覆盖线上 0.3.7；正式 Update 公钥、代码签名证书和图标仍待外部材料。

### 2026-07-30 - 完成签名灰度与暂停发布

- 更新 manifest 升级到 v2，签名覆盖 rollout 状态、基点、固定 seed 和策略更新时间。
- 新上传版本默认暂停；管理后台可按 5%、25%、100% 单调扩大 cohort，或保留比例立即暂停。
- 每次策略变化分配更高全局 sequence；Desktop 对 paused/未命中也落防回滚状态。
- 用户确认安装后、运行时停机前再次验签；下载期间暂停、换版或退出 cohort 时不停止当前聊天。
- Portal 只有 100% active 才显示通用下载，灰度与暂停显示对应状态；灰度不作为授权控制。
- 自动更新稳定、灰度、暂停闭环已完成；二进制程序回滚当时留给 LH-040-08，现已完成。
- 全仓 57 个文件、250 项测试通过，4 项 PostgreSQL 条件跳过；OpenAPI 29 条路径和三项门禁通过。

### 2026-07-29 - 完成稳定渠道安全自动更新事务

- Electron Main 仅在安装包内信任清单审批通过后启用，仓库默认 pending 清单不会在线自举公钥。
- 原生双确认后受限下载，执行前同时验证 Ed25519 元数据、大小/SHA-256、Authenticode 主体与时间戳。
- 安装前停止 Core/Gateway/Bridge，保存固定 LongHub 状态快照；新版本真实 WebUI 健康后清除 pending。
- 公开构建门禁核对信任清单审批、签名主体和 ASAR 逐字节一致性；测试/私钥不得进入生产信任锚。
- 全仓 57 个文件、246 项测试通过，4 项 PostgreSQL 条件跳过；三项代码门禁通过。
- 灰度/暂停策略与上一稳定版二进制回滚拆为 LH-040-07、LH-040-08；该轮未部署或重打候选。

### 2026-07-29 - 建立客户端更新签名、防回滚与密钥轮换

- 新增 `longhub/client-update/v1` 严格 manifest 与独立签名用途域，Update key 禁止与 Pack key 复用。
- Cloud 上传时流式计算大小/SHA-256、分配单调 sequence，禁止覆盖和降级；legacy 元数据不自动重签。
- Desktop 只接受预置公钥，持久化各渠道最高 sequence，拒绝重放、同序列异文和跨源下载 URL。
- 安装包执行前流式验证签名绑定的大小与 SHA-256；OpenAPI、Admin、Portal 与 Nginx 下载路径已同步。
- 正式 Update 公钥、代码签名证书和正式图标仍待外部材料；本项未接自动安装，也未覆盖线上 0.3.7。

### 2026-07-29 - 启用 ASAR 并锁定外置运行时闭包

- Electron Main、Renderer、激活资产、Core 与 Worker 进入 `app.asar`，不再保留 `resources/app`。
- 独立 Node 必需的 OpenClaw、Tool Bridge 及生产依赖以 289 包白名单外置，跨平台可选原生包不入包。
- OpenClaw 升级导致依赖图变化时构建 fail-closed，要求显式重生成和审查运行时清单。
- 打包验收使用安装目录的 Electron/Node 真实启动 Core、Worker、Bridge 和 Gateway `/chat`。
- 0.4.0 内部候选安装器 SHA-256 为
  `1E78823925D5F343D84CF1CBDE6649D676D2D892A4BC76EBB6F316AC397479DB`。

### 2026-07-29 - 建立统一日志与审计脱敏

- `@longhub/observability` 增加递归敏感字段、自由错误文本、异常对象和大小限制处理。
- Desktop 生产入口移除直接 `console.*`，OpenClaw 子进程错误在日志或用户诊断显示前统一清理。
- Cloud Memory/PostgreSQL 审计详情落库前再次脱敏，避免未来路由误传敏感字段形成长期泄露。
- 回归测试使用固定哨兵覆盖设备 Token、Gateway Token、API Key、授权码和用户文件内容。
- 共享脱敏 4 项、Cloud 50 项和 Desktop 115 项全量回归通过；全仓类型、Lint、构建和安全门禁通过。
- 重打 0.4.0 内部候选并用打包内置 Node 验证实际脱敏模块；安装器 SHA-256 为
  `D3AD1A368C7199263F39568CB6F8EDB4396B20B8F267366AFCD7941CC6A452E8`。

### 2026-07-29 - 设备凭据迁移到 Windows Credential Manager

- Desktop 使用 Win32 Credential API 保存当前 Windows 用户的设备 Token，Target 只包含规范 URL 摘要。
- `device.json` V2 只保留指纹与设备 ID；旧 Token 只有在写入并回读一致后才原子清除。
- 失败、超时、回读不一致、旧文件损坏和多地址全量迁移均保持失败安全，不会静默换设备或误删授权。
- Cloud 激活状态向已认证设备返回自己的 `device_id`，兼容早期只保存 Token 的客户端；服务端先部署。
- 真实 Windows Credential Manager 读写删除、Cloud 49 项和 Desktop 114 项全量回归均通过。

### 2026-07-29 - 建立 Windows 正式发布门禁

- 将 `dist:internal` 与默认正式 `dist` 分离，内部候选不会再被默认命令误认为正式包。
- 新增品牌审批清单和 SVG/PNG/ICO 摘要、尺寸及多分辨率检查；当前临时图标明确不能正式发布。
- 正式包必须通过安装器和主程序 Authenticode 主体、有效状态及可信时间戳检查。
- 内置 Node 必须保留 OpenJS Foundation 有效签名并满足锁定 OpenClaw 的版本范围。
- 当前外部依赖仍是可信代码签名证书、预期 Subject 和正式视觉稿，因此 LH-040-01 保持进行中。

### 2026-07-29 - 完成首次授权码激活门禁

- 新设备注册后只获得设备身份，必须核销后台签发的授权码才可启动模型、Gateway 和主 WebUI。
- 授权码使用 128-bit 随机值和摘要存储，支持有效期、最大设备数、Pack 附带和即时撤销。
- 模型与受保护产品 API 服务端逐请求复验，换码会清除旧码附带 entitlement。
- 独立 Electron 激活页保持 sandbox、最小 preload 和精确 IPC 来源校验；真实窗口 E2E 已通过。
- 当前遗留为生产 nginx IP/全局限流和运行中自动回退激活页。

### 2026-07-29 - 完成龙枢 UI 产品化

- 窗口、侧边栏、移动顶栏、面包屑、欢迎页和错误页统一为龙枢品牌与中文文案。
- 新增可复现生成的临时“龙”图标，同时用于页面、BrowserWindow、应用和 NSIS 快捷方式。
- 默认 `Main Session` 显示为“龙枢助手会话”，会话悬停标题不再包含 `agent:` 内部 key。
- 隐藏管理导航、设置/文档、命令搜索和管理员欢迎建议，Main 只允许产品 `/chat` 路由。
- 产品化脚本不遍历聊天正文、没有 preload/IPC，并用真实 Gateway + Electron 验证动态重渲染。
- 全仓 26/26 测试任务、Desktop 25 文件 98 项、29/29 类型检查与 lint 通过。

### 2026-07-29 - 完成版本化 OpenClaw 兼容层

- 新增 `@longhub/openclaw-compat`，集中精确版本、路由、DOM 语义、RPC、`replacePaths` 和 UI 基线。
- Desktop 从实际内置包回读 OpenClaw 版本；缺失、损坏或漂移时拒绝启动并显示诊断错误。
- 真实 Gateway + Electron 合约覆盖 Agent Selector、模型/管理入口隐藏、宿主方法与已知产品化缺口。
- 固定 1200 × 800 视口与 420 × 220 稳定区域，采用颜色、明暗和边缘密度容差签名降低动态像素误报。
- 全仓 26/26 测试任务、29/29 类型检查和 29/29 lint 通过；后续转入 LH-037-02。

### 2026-07-29 - 完成已授权未安装 HR 一键安装

- 全新 userData 组合云端 catalog 与设备 entitlement，原生 Selector 直接显示“HR 助理（点击安装）”。
- 单次点击完成制品/公钥下载、响应元数据核对、Ed25519 验签、公钥原子持久化、Pack 激活、
  `agents.list` 热更新与 HR 专属会话进入。
- 云端目录只筛选客户端锁定认识的 Pack；不能注入 profileId、agentId、安装 URL 或本机命令。
- 固定安装导航、失败重试、无 preload/Node 边界通过真实 Electron；完整激活通过真实 OpenClaw
  Gateway 回读 `main + HR`。
- 全仓 typecheck 27/27、test 24/24、lint 27/27 通过；Desktop 24 文件、96 项测试通过。
- 重打 0.3.6 内部候选包并回读 Node 25.9.0、OpenClaw 2026.7.1-2、Bridge 与一键安装产物；
  SHA-256 为 `7FA5B45B3A5939D35EF0E4B02B0A1B164090F201BC56F4EDD666C43C662DB3FF`。

### 2026-07-29 - 完成 0.3.6 候选版状态连续性与共存验收

- 新增同一 userData 上的 0.3.5 状态 → 0.3.6、Pack 升级、重启与回滚连续 E2E。
- 同时启动用户 OpenClaw 与龙枢内置真实 Gateway，验证端口避让、状态隔离和龙枢重启连续性。
- 生成内部 0.3.6 候选安装包；内置 Node 25.9.0、OpenClaw 2026.7.1-2 和 Tool Bridge 已回读。
- 全仓 typecheck 27/27、test 24/24、lint 27/27 通过；Desktop 20 文件、87 项测试通过。
- 未签名和默认 Electron 图标继续作为 LH-040-01 公开发布阻断项，不把内部候选版标记为正式版。

### 2026-07-29 - 完成原生 Agent Selector 一键切换 E2E

- 增加启用 Agent 允许列表；历史会话保留，但停用/撤销 Agent 不再重新出现在 Selector。
- 上游未渲染 Selector 时复用原生样式和 `selectAgent()` 路由补齐入口，不创建第二套聊天面板。
- 首次点击恢复目标 Agent 最近会话；无历史时使用独立 `agent:<agentId>:main`。
- 活动执行中必须确认停止，Stop 消失后才切换；撤销当前 Agent 显式回退 `agent:main:main`。
- 锁定版 Gateway + Electron 点击 E2E 和身份/workspace/memory/工具/会话隔离报告已完成。

### 2026-07-29 - 完成 Pack 运行时启停和授权撤销闭环

- 新增生命周期协调器，串行管理 Registry、Core Bridge policy 与 OpenClaw `agents.list`。
- 启用前重新校验 active Pack 摘要、兼容性、保存的 Ed25519 签名与云端 entitlement/release。
- entitlement 默认 30 秒同步；确认撤销时先关闭 Core 执行边界，再移除 Selector 并回退 `main`。
- 配置提交或回读失败会补偿恢复旧 Gateway/Core/Registry；workspace/session 和 active 指针不删除、不误改。
- 锁定版 OpenClaw 已真实验证 `config.get/config.patch/agents.list` 往返与恢复。

### 2026-07-29 - 一键智能体切换提升为近期主线

- 复用现有 Pack 分发、HR Pack、Core/Worker 和 OpenClaw 原生多 Agent，不重复开发聊天面板。
- 将 0.3.6 调整为 `main + HR` 双智能体纵向 MVP，补齐 Agent Profile、Registry、Config Composer、Tool Bridge 和权限闭环。
- 将品牌与中文化移动到 0.3.7；0.6.0 聚焦知识库、能力生态与市场。
- 增加 Agent 隔离、授权实时撤销和一键切换的发布门禁。

### 2026-07-29 - 完成 Agent Profile V1

- 新增严格 Profile Schema、Manifest/Profile/引用文件联合校验和正式契约。
- HR Pack 从未校验 `agent.yaml` 迁移为签名保护的 `agent-profile.json` 与 workspace 模板。
- Pack 摘要改为覆盖 Manifest 与全部文件，云端发布和桌面安装均执行联合校验。

### 2026-07-29 - 完成 Agent Registry 与 Config Composer

- 新增原子持久化 Registry，稳定映射 Pack/Profile/OpenClaw agentId 和版本，禁止 Pack 劫持已有 Profile。
- Profile ID 使用规范化名称加 SHA-256 后缀生成 agentId，显示名和版本变化不会改变会话归属。
- Composer 确定性生成 `main + 已启用 Profile` 的 `agents.list`，固定后台模型策略并关闭跨 Agent 工具。
- 生成配置已通过 OpenClaw 2026.7.1-2 `config validate` 真实校验；运行时激活与 workspace 落盘进入 LH-036-03。

### 2026-07-29 - 完成 main + HR 启动时双智能体激活

- PackInstaller 增加 active 制品安全读取，拒绝指针路径穿越、符号链接、内容和摘要篡改。
- Desktop 在 Gateway 启动前注册 active Profile，落盘独立 workspace/agentDir/session，并写入完整 `agents.list`。
- USER 模板只做首次初始化；重复启动保持用户偏好和 Registry revision 稳定。
- `main + HR` 配置已通过真实 OpenClaw 校验和 Gateway `/chat` 冒烟，锁定版 Selector DOM 合约已纳入测试。

### 2026-07-29 - 完成 LongHub Tool Bridge POC

- 新增独立 `@longhub/openclaw-bridge` Tool Plugin，使用 OpenClaw factory 上下文绑定可信 agent/session。
- 工具 schema 不接受身份或权限字段；缺少可信上下文时不创建工具，Core 也会按已激活 Profile 策略再次拒绝。
- Desktop 启动随机令牌保护的 `127.0.0.1` 单路由 Bridge Host，Gateway 只通过子进程环境取得临时凭据。
- HR 第一阶段只开放只读 `longhub_resume_screen`；写操作、JD 递归路径和云端技能等权限闭环后再开放。
- 插件通过 OpenClaw 2026.7.1-2 `plugins validate` 与 `plugins inspect --runtime`，并新增 7 个 Bridge/Core 边界测试。

### 2026-07-29 - 建立路线基线

- 以 0.3.5 为稳定起点，规划产品化、稳定安全、后台运营和企业能力四个阶段。
- 建立 OpenClaw 精确锁版、兼容矩阵、升级门禁、灰度和回滚规范。
- 将 0.3.6 的近期工作拆分为可跟踪任务。
