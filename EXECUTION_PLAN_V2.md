# LongHub V2 后续开发执行计划

> 文档状态：有效，后续开发唯一进度源  
> 适用日期：2026-07-30 起  
> 当前线上稳定版：LongHub Desktop 0.4.1  
> 当前内置底座：OpenClaw 2026.7.1-2  
> 计划目标：完成一键智能体与官方能力闭环，再分阶段开放用户能力  
> 详细设计参考：[平台设计](DESIGN.md)、[功能开放策略](PRODUCT_FEATURE_POLICY.md)、
> [Skill 设计](SKILL_PLATFORM.md)、[Agent 平台设计](AGENT_PLATFORM.md)

## 一、文档权威与使用方式

本文取代旧 [ROADMAP.md](ROADMAP.md) 中 0.7 以后尚未开始的任务，作为后续开发、验收和“下一步”的
唯一执行顺序。旧路线只保留 0.3.6—0.6 已完成能力、历史证据和 1.0 外部阻断记录，不再新增未来任务。

执行规则：

1. 默认从第一个“待开始”任务继续，不跨过未满足的阻断前置条件。
2. 一个任务只有同时具备实现、测试、文档和验证证据，才可标记“代码完成”。
3. “代码完成”不等于已经打包、部署、发布或对用户开放；五种状态必须分开记录。
4. 安全、数据损坏、授权绕过和发布阻断可以插队，但需要在本文风险与变更记录中说明。
5. 每个版本冻结后不继续塞入新功能；非阻断需求进入下一版本。

## 二、产品主线与范围

### 2.1 主线

龙枢的核心不是复制完整 OpenClaw 控制台，而是：

- 安装后直接进入产品化的原生聊天界面。
- 用户一键切换不同功能智能体。
- 模型、Provider、API Key、额度和发布策略由后台统一管理。
- 官方 Agent/Skill 可以安全安装、启停、升级、回滚和撤销。
- 每个 Agent 的身份、会话、记忆、文件和工具权限互不串用。
- Core 在最终执行点复验身份、授权、权限、预算和确认。

### 2.2 1.0 必须完成

- Feature Policy V2、产品扩展面和可信确认中心。
- 官方 Skill 目录、Agent 绑定和完整生命周期。
- 会话管理、本机搜索/导出和可恢复删除。
- 安全文件输入、企业知识引用和设备本地个人资料。
- 用户无代码 Content Skill、受限 Workflow、无代码 Agent 和摘要转交。
- Windows 正式签名、外部安全评审、生产密钥/恢复演练和观察期指标。

### 2.3 1.0 不纳入

以下能力保留在 1.x，不阻塞首个正式商用版本：

- 语音输入/TTS、图片生成、完整多模态媒体服务。
- 企业消息渠道、邮件发送、定时企业任务和 Webhook。
- 自动 Agent 路由、多 Agent 自主编排和组织内分享。
- 用户账号/OIDC、跨设备会话同步、公开分享和多人实时协作。
- 第三方云端可执行代码、本地 WASM、任意插件/MCP/脚本/原生程序。

## 三、当前事实基线

| 能力 | 代码状态 | 线上/用户状态 | V2 处理 |
|---|---|---|---|
| Desktop 0.4.1 激活、聊天、恢复和更新 | 已完成 | 0.4.1 已上线 | 保持回归，不重做 |
| 后台模型分层、额度、计量和设备运营 | 已完成 | Cloud 已部署 | 继续作为模型与费用底座 |
| Agent Catalog、Pack 审核、知识查询 | 后台基础已完成 | 后台可用，普通用户入口不完整 | V2 接入产品入口 |
| Agent Registry、Selector、Tool Bridge | 已完成 | 随 0.4.1 客户端提供 | 扩展 Skill，不改变可信上下文 |
| Core 权限交集和确认记录 | 核心逻辑已完成 | 只开放只读工具；没有成品确认交互 | 0.5 补确认中心 |
| `file_upload` 开关 | 已解析 | 尚未形成完整文件链 | 0.7 才允许真正开放 |
| Skill Worker | 静态可信 Skill Map | 不支持第三方动态安装 | 1.0 前不开放本地代码 |
| 第三方 Pack 扫描 | 基础 Schema/危险模式扫描 | 可作提交准入，不是代码安全证明 | 第三方执行延后到 1.x |
| 用户身份 | 设备授权码 + 租户 | 没有稳定个人账号 | 1.0 个人内容限定设备本地 |

### 状态定义

| 状态 | 含义 |
|---|---|
| 待开始 | 尚未进入实现 |
| 进行中 | 正在开发或验证 |
| 代码完成 | 实现、测试、文档和本地证据齐全 |
| 候选完成 | 安装包/镜像完成并通过发布门禁，尚未灰度 |
| 已部署 | Cloud/Admin 等服务已部署，不代表客户端用户已获得入口 |
| 已发布 | 客户端已按签名渠道灰度或全量发布 |
| 已开放 | 后台策略已经对指定用户范围开启 |
| 阻塞 | 存在明确外部依赖，当前不能继续 |

## 四、先行架构决策

### 4.1 Feature Policy V2 必须先于新入口

`longhub/feature-policy/v2` 是 Skill、文件、知识、工作流和未来连接器的共同前置条件。策略至少包含：

```text
feature_id / enabled / scope / audience / mode
limits / data_policy / required_entitlements / required_permissions
min_desktop_version / max_desktop_version
policy_version / issued_at / expires_at / emergency_disabled
```

解析和合并规则固定为：

- 任一上级策略禁用则禁用，低层作用域不能重新放开。
- 风险模式取最高：`admin_approved > tenant_controlled > default`。
- 数量、字节、时长、并发和费用上限取所有适用策略中的最小值。
- entitlement 和 permission 取交集，不使用最具体记录覆盖全部上级约束。
- Agent scope 是额外收紧维度，不参与 `device > plan > tenant > global` 的简单替换。
- 普通用户设置只能收紧，不能提高上限或改变风险模式。
- 高风险功能无法在线复验时安全失败；低风险 UI 可以显示最后一次未过期状态。

Cloud API 在每次受保护调用时按同一策略复验。Desktop 至少每 30 秒或通过失效通知刷新显示；紧急关闭
先在服务端阻断执行，再更新 UI。旧 runtime-config 十分钟缓存不能授权企业写入或第三方执行。

Feature Policy 使用独立 `/v1/client/feature-policy` 端点，不把现有严格 `longhub/runtime-config/v1`
原地替换为新 Schema。Cloud 必须先部署并继续向 Desktop 0.4.1 提供完全兼容的 runtime-config v1；新
Desktop 再并行读取 Feature Policy V2。支持窗口内不得给 v1 响应添加旧客户端会拒绝的未知字段。

### 4.2 Product Extension Surface V1

主窗口继续直接显示 `/chat`，但不把越来越多功能塞进不受控 DOM 补丁。产品扩展面采用：

- OpenClaw 页面只注入版本化、无 Node/IPC 的入口按钮。
- “智能体”“能力”“我的”打开龙枢签名资源中的受限抽屉或子窗口。
- 子窗口使用独立 origin、Context Isolation、sandbox 和最小 preload。
- 每个本机动作使用单用途 IPC；请求绑定窗口来源、动作类型和一次性 nonce。
- 文件、保存、麦克风和截图返回一次性能力句柄，不把任意路径或通用系统 API 暴露给页面。
- Main 导航白名单继续禁止模型、Gateway、Channels、MCP、插件和基础设施路由。

如果锁定版 OpenClaw 无稳定挂载点，优先使用独立受限子窗口，不扩大 DOM/CSS 注入面积。扩展入口、
路由和视觉基线必须进入 `@longhub/openclaw-compat`。

### 4.3 Confirmation Center V1

Core 已有绑定参数摘要的一次性确认记录，但产品交互尚未闭环。确认中心必须：

- 展示目标 Agent、Skill、动作、对象、接收方、数据范围和费用摘要。
- 只接受 Core 生成的确认 ID，不接受模型或 Skill 自行生成。
- 绑定 Agent、Profile 版本、Session、ToolCall、permissions 和 payload digest。
- 五分钟过期、一次性消费；参数、Agent、版本或会话变化后重新确认。
- 拒绝、窗口关闭、超时和离线都视为未授权。
- 确认记录不在不同工作流步骤或不同 Agent 之间复用。

在确认中心验收前，Workflow 只能调用零权限或免确认的只读 Skill。

### 4.4 身份和个人数据

1.0 继续使用设备授权码，不强制个人账号：

- “个人知识”“个人模板”“个人偏好”在 1.0 只表示当前设备、当前 Windows 用户和当前 Agent 的本地数据。
- Cloud 只处理租户知识和管理员配置，不创建伪个人云空间。
- 组织分享、跨设备同步、个人云导出删除和高风险企业写操作进入 Principal V2/OIDC 后实施。
- 审计中只有设备身份时必须明确标记 actor 类型，不能伪装成自然人审计。
- 1.0 Workflow 只允许只读、零权限或本机可恢复操作；企业系统写入、对外发送、删除、付款和权限变更
  等待 Principal V2/OIDC 后开放。

### 4.5 Skill 的安装语义

第一阶段区分三种状态：

| 类型 | 用户动作 | 运行位置 | 含义 |
|---|---|---|---|
| 内置官方本地 Skill | 启用/停用 | 已签名 Desktop Worker | 实现已随客户端存在，不下载代码 |
| Content/Workflow Skill | 安装/升级 | 声明解释器/Core | 下载严格数据制品，不含任意代码 |
| 官方 CloudRef Skill | 安装引用/启用 | Cloud Executor | 本机只保存签名引用，不下载服务端实现 |

Catalog 不得把“启用内置能力”描述成“下载本地代码”。Skill Package V1 不包含 JS、Python、PowerShell、
EXE/DLL、OpenClaw 插件、MCP server 或任意原生入口。

## 五、版本路线

### 0.5.0：平台开放基础

目标：先建立后续所有功能共同依赖的策略、UI 扩展与确认边界。

- Feature Policy V2 Schema、Store、Admin、OpenAPI、Desktop 解析和服务端执行复验。
- 新增独立 Feature Policy 端点并保持 runtime-config v1 向后兼容，Cloud 先于 Desktop 发布。
- 策略 deny-wins/最小限制合并、30 秒 UI 刷新和紧急停用。
- Product Extension Surface V1 与 OpenClaw 兼容契约。
- Confirmation Center V1 与只读/本机可恢复写入安全回归。
- 版本状态拆分和统一发布证据模板。
- OpenClaw 会话 API 可行性 spike（并行调研，不写生产代码，结论直接决定 0.7 会话能力范围）。

发布门槛：关闭策略的 UI、直接路由和 API 均不可用；旧页面、旧缓存和历史会话不能绕过；确认不可
伪造、重放或跨 Agent 使用；主 WebUI 仍无 preload/Node/通用 IPC。

### 0.6.0：官方 Skill 中心

目标：普通用户可以为指定 Agent 安全启停官方能力，不开放第三方本地代码。

- Skill Package V1、Publisher namespace、Skill Registry 和 Agent-Skill Binding。
- 官方 Catalog、详情、兼容、费用、权限和执行位置展示。
- 内置 Skill 启停、CloudRef 安装引用、升级、回滚、撤回和卸载。
- “能力”入口与 Agent 绑定选择。
- Core policy 原子更新、Skill 级 entitlement、计量和审计。
- OpenClaw 纯内容 Skill 的隔离导入评估；脚本/插件/MCP 继续拒绝。

发布门槛：安装失败原子恢复；撤销立即阻断已打开会话；A Agent 的 Skill/确认不能在 B Agent 使用；
Catalog 不能让未知运行时实现进入 Worker。

### 0.7.0：文件、知识与会话能力

目标：让智能体真正处理用户资料，同时保持文件和企业数据边界。

- 会话重命名、归档、置顶、本机搜索、单会话导出和可恢复删除。
- 原生文件选择、拖放/附件暂存、类型/大小/数量限制和一次性文件句柄。
- 文件解析隔离进程、超时/资源上限、恶意文档和压缩炸弹拒绝。
- 企业知识查询、Agent/租户范围、引用、更新时间和删除证明。
- 当前设备本地个人资料库；不上传为个人云知识。
- 模型输入能力协商：文本提取与图片理解分别判断，后台仍统一选模。

会话操作优先使用锁定版 OpenClaw 公开 RPC/API，可行范围以 0.5 期间 `LH-V2-008` spike 的结论为准。
龙枢可以维护自己的只读搜索/展示索引，但不能直接改写 OpenClaw SQLite、transcript 或会话内部文件；
如果某项操作没有稳定公开接口，该项延期并向上游提出扩展需求，不能以未版本化数据补丁通过 0.7 门禁。

发布门槛：路径穿越、符号链接、私网 URL、超大文件和解析器耗尽均拒绝；临时文件按保留策略清理；
会话/文件/知识不跨 Agent、用户目录或租户；模型不支持视觉时 UI 不显示图片理解。

### 0.8.0：无代码智能体与受限工作流

目标：用户可以组合已有官方能力，但工作流不成为新的权限来源。

- Content Skill 创建、导入、导出和用户来源标记。
- 受限 Workflow DSL：顺序、有限分支、固定次数循环、人工确认和官方 Skill 调用。
- 每个子 Skill 逐次鉴权、确认、预算和计量；禁止递归、动态代码和无限后台任务。
- 1.0 只允许只读、零权限或本机可恢复子 Skill；企业外部副作用进入 1.2。
- 无代码 Agent：名称、说明、用户偏好、设备本地知识和已授权 Skill。
- 跨 Agent 摘要转交：用户预览确认，目标 Agent 不继承来源权限或原始记忆。
- 纯内容 OpenClaw Skill 降权转换；未知引用和可执行内容拒绝。

发布门槛：Workflow 最大 20 步、深度 3；失败/取消不会重复本机副作用；确认不跨步骤复用；任何
企业外部写入均被策略拒绝；用户来源内容
不能冒充官方签名发布方；无代码 Agent 不能声明模型、Gateway、插件或权限。

### 0.9.0：正式商用加固

目标：冻结功能，不再增加大能力，完成正式发布的外部和运营门槛。

- 可信 Authenticode 证书、正式 Logo/图标和品牌审批摘要。
- 目标 Windows 版本的全新安装、升级、回滚、卸载和企业环境矩阵。
- 独立渗透测试/外部安全评审，高危与严重问题清零。
- 生产 KMS、Pack/Update/模型/知识密钥轮换和权限隔离。
- PostgreSQL/制品/知识数据备份恢复及服务故障演练。
- 正式用户协议、隐私说明、处理区域、供应商、保留期和法务批准。
- 5% → 25% → 100% 灰度与连续观察期。

### 1.0.0：正式商用

只有 0.5—0.9 全部发布门槛完成、没有未处理高危/严重问题、线上观察指标达标，才发布 1.0.0。
1.0 不因为语音、图片、企业渠道或第三方市场尚未完成而延期。

### 1.x：扩展能力

按独立版本逐项推进，不并行塞进 1.0：

1. `1.1`：安全网页搜索、URL 摘要、视觉输入、语音输入/TTS、图片生成；先增加模型 capability registry
   和独立媒体 API，不复用聊天布尔开关冒充支持。
2. `1.2`：Principal V2/OIDC、企业 Connector、邮件/消息渠道和参数绑定写操作。
3. `1.3`：本地提醒、云端定时只读任务、签名 Webhook 和可见可取消的多 Agent DAG。
4. `1.4`：组织内 Agent/Skill/Workflow 分享、第三方 Cloud Skill 审核与隔离执行。
5. 更后期：跨设备会话同步、多人协作、本地 WASM。任意脚本、原生程序和用户自建 MCP 不在计划内。

## 六、执行任务表

### 0.5 平台基础

| 顺序 | ID | 任务 | 前置 | 状态 | 完成证据 |
|---:|---|---|---|---|---|
| 1 | LH-V2-001 | 冻结 Feature Policy V2 Schema、独立端点与合并算法 | 现有 runtime-config | 待开始 | Schema、v1 兼容、deny-wins、限制取最小、未知字段测试 |
| 2 | LH-V2-002 | 实现 Cloud Store/Admin/OpenAPI 与逐请求复验 | 001 | 待开始 | Cloud 先行兼容部署、Memory/PG、管理审计、直接 API 绕过测试 |
| 3 | LH-V2-003 | 实现 Desktop 解析、刷新、缓存与紧急失效 | 001、002 | 待开始 | 30 秒刷新、离线边界、旧缓存与撤销 E2E |
| 4 | LH-V2-004 | 冻结 Product Extension Surface V1 | OpenClaw compat | 待开始 | 路由、origin、窗口、最小 IPC 与威胁模型 |
| 5 | LH-V2-005 | 实现“智能体/能力/我的”受限入口骨架 | 003、004 | 待开始 | 无 Node/通用 IPC、直接路由和视觉 E2E |
| 6 | LH-V2-006 | 完成 Confirmation Center V1 | Core 确认记录、004 | 待开始 | 参数绑定、过期、拒绝、重放、跨 Agent 测试 |
| 7 | LH-V2-007 | 0.5 全量回归与候选发布门禁 | 001—006 | 待开始 | 单元、契约、真实 Gateway/Electron、安装回滚证据 |
| 8 | LH-V2-008 | OpenClaw 会话 API 可行性 spike（只调研不实现） | 004 | 待开始 | 锁定版公开 RPC/API 能力清单、缺口列表与上游扩展需求草案 |

0.5 可并行推进的分组（缩短关键路径，不改变依赖约束）：

| 并行组 | 任务 | 说明 |
|---|---|---|
| A 策略线 | 001 → 002 → 003 | Cloud 先行兼容部署，Desktop 解析随后 |
| B 扩展面线 | 004 → 005 | 005 仍需等 003 提供策略解析后合流 |
| C 确认线 | 006 | 只依赖 Core 确认记录与 004，不必等 003 |
| D 调研线 | 008 | 与 A/B/C 完全并行，结论供 0.7 的 020 使用 |

约束不变：005 合流需要 003 完成；007 需要 001—006 全部完成。008 只产出结论文档，不写生产代码，
必须在 0.6 冻结前给出“有稳定公开接口 / 需上游扩展 / 需延期”三态判定。

### 0.6 官方 Skill

| 顺序 | ID | 任务 | 前置 | 状态 | 完成证据 |
|---:|---|---|---|---|---|
| 9 | LH-V2-010 | 冻结 Skill Package V1 与发布方命名空间 | 001、现有 Pack Schema | 待开始 | 严格 Schema、禁止字段、ID 所有权测试 |
| 10 | LH-V2-011 | 实现 Skill Registry 与 Agent-Skill Binding | 010、Agent Registry | 待开始 | 原子存储、所有权、迁移和损坏恢复测试 |
| 11 | LH-V2-012 | 实现官方 Catalog、详情和签名引用分发 | 002、010 | 待开始 | entitlement、兼容、签名、撤回和审计 E2E |
| 12 | LH-V2-013 | 区分内置启用、声明安装和 CloudRef | 011、012 | 待开始 | 未知 runtime 拒绝、UI 语义和 Worker allowlist 测试 |
| 13 | LH-V2-014 | 实现 Skill 启停、升级、回滚和卸载事务 | 011—013 | 待开始 | Core/Gateway/Registry 补偿回滚与撤销即时阻断 |
| 14 | LH-V2-015 | 完成“能力”入口、权限/费用预览和 Agent 绑定 | 005、014 | 待开始 | 真实 Electron 安装、启停、失败重试和跨 Agent E2E |
| 15 | LH-V2-016 | 0.6 供应链与发布门禁 | 010—015 | 待开始 | 恶意包、重放、兼容、升级回滚和外部文件清单 |

### 0.7 文件、知识和会话

| 顺序 | ID | 任务 | 前置 | 状态 | 完成证据 |
|---:|---|---|---|---|---|
| 16 | LH-V2-020 | 会话索引、重命名、归档、搜索和导出 | 005、008 结论 | 待开始 | 公开 API、只读索引、Agent 隔离和导出边界测试 |
| 17 | LH-V2-021 | 可恢复删除与明确保留策略 | 020、现有存储维护 | 待开始 | 回收、恢复、永久删除确认和升级连续性 |
| 18 | LH-V2-022 | 一次性文件能力句柄与附件暂存 | 004、005 | 待开始 | 来源绑定、路径/链接拒绝、取消和清理测试 |
| 19 | LH-V2-023 | 文件解析隔离与资源门禁 | 022 | 待开始 | 类型/大小、压缩炸弹、超时、崩溃和恶意样本测试 |
| 20 | LH-V2-024 | 企业知识查询接入 Agent 与引用 UI | 002、005、现有知识 API | 待开始 | 租户/Agent 隔离、引用、删除和真实聊天 E2E |
| 21 | LH-V2-025 | 当前设备本地个人资料库 | 022、023 | 待开始 | 不上传、Windows 用户/Agent 隔离、删除恢复测试 |
| 22 | LH-V2-026 | 模型输入 capability 协商与安全降级 | 001、023 | 待开始 | 文本/视觉区分、不支持时隐藏、后台强制选模 |
| 23 | LH-V2-027 | 0.7 文件/知识/会话总门禁 | 020—026 | 待开始 | 路径、SSRF、资源耗尽、数据隔离和安装包 E2E |

### 0.8 无代码能力

| 顺序 | ID | 任务 | 前置 | 状态 | 完成证据 |
|---:|---|---|---|---|---|
| 24 | LH-V2-030 | Content Skill Schema、解释器和来源标记 | 010、015 | 待开始 | 无代码、零默认权限、导入导出和冒充拒绝 |
| 25 | LH-V2-031 | 受限 Workflow DSL 与静态验证器 | 006、030 | 待开始 | 步数/深度/循环、递归/动态代码拒绝测试 |
| 26 | LH-V2-032 | Core 子步骤鉴权、预算、确认和幂等 | 006、031 | 待开始 | 外部写入拒绝、确认不复用、取消/重试无重复本机副作用 |
| 27 | LH-V2-033 | 无代码 Agent 用户覆盖层 | 011、025、030 | 待开始 | 签名 Profile 不变、模型/权限/插件禁止字段 |
| 28 | LH-V2-034 | 跨 Agent 摘要预览与确认转交 | 006、033 | 待开始 | 不传原始记忆/权限、目标绑定和取消测试 |
| 29 | LH-V2-035 | OpenClaw 纯内容 Skill 降权导入 | 030 | 待开始 | 脚本、插件、MCP、远程加载和扩展名伪装拒绝 |
| 30 | LH-V2-036 | 0.8 无代码能力总门禁 | 030—035 | 待开始 | 恶意内容、跨 Agent、工作流恢复和真实 UI E2E |

### 0.9 与 1.0 商用门禁

| 顺序 | ID | 任务 | 前置 | 状态 | 完成证据 |
|---:|---|---|---|---|---|
| 31 | LH-V2-040 | 可信代码签名证书与正式品牌资产 | 外部采购 | 阻塞 | Subject、时间戳、图标摘要和正式安装包 |
| 32 | LH-V2-041 | Windows 安装/升级/回滚/卸载矩阵 | 007、016、027、036、040 | 待开始 | 目标系统、企业策略、断网/重启和恢复报告 |
| 33 | LH-V2-042 | 生产 KMS、密钥轮换和最小权限 | 运维/KMS | 阻塞 | Pack/Update/模型/知识独立密钥演练 |
| 34 | LH-V2-043 | 数据库、制品和知识备份恢复演练 | 042 | 待开始 | RPO/RTO、租户隔离、恢复校验和删除说明（生产库每日 pg_dump 已先行上线） |
| 35 | LH-V2-044 | 独立渗透测试与高危关闭 | 041、生产候选 | 阻塞 | 外部报告，高危/严重为 0 |
| 36 | LH-V2-045 | 正式协议、隐私、供应商和保留期批准 | 运营/法务 | 阻塞 | 正式批准文本与产品展示证据 |
| 37 | LH-V2-046 | 内部 → 5% → 25% → 100% 灰度观察 | 041—045 | 待开始 | 成功率、崩溃率、模型、更新和回滚指标 |
| 38 | LH-V2-047 | 发布 LongHub 1.0.0 | 046 | 待开始 | 签名安装包、发布索引、回滚版本和上线验收 |

外部阻断项的“启动时间”与“排期位置”分离：证书、KMS、渗透测试和法务的采购/选型/合同周期以月计，
必须现在并行启动，只是验收落在 0.9。

| ID | 排期位置 | 启动时间 | 现在就要做的动作 |
|---|---|---|---|
| LH-V2-040 | 0.9 | 立即（2026-07） | 选定 CA、准备主体材料、走 Authenticode 签发流程；同步品牌资产设计 |
| LH-V2-042 | 0.9 | 立即（2026-07） | 选定 KMS 方案、确认密钥分层与权限模型、预算审批 |
| LH-V2-044 | 0.9 | 0.7 期间预约 | 询价并锁定外部安全团队档期，约定测试范围与复测条款 |
| LH-V2-045 | 0.9 | 立即（2026-07） | 提交协议/隐私/数据处理区域草案给法务，进入评审排队 |

## 七、1.0 验收指标

| 类别 | 指标 |
|---|---:|
| 全新安装后进入可聊天页面 | 60 秒内；常规启动 15 秒内 |
| 无配置首次聊天成功率 | ≥ 99% |
| 已安装 Agent 切换 | ≤ 3 秒 |
| Agent 身份/会话/记忆/文件/工具串用 | 0 |
| 官方 Skill 安装/启停成功率 | ≥ 99% |
| 策略紧急关闭服务端生效 | 立即；UI 30 秒内同步 |
| 撤销后新 Skill 执行 | 100% 拒绝 |
| 确认伪造、重放和跨 Agent 复用 | 100% 拒绝 |
| 文件恶意样本、路径穿越、符号链接越界 | 100% 拒绝 |
| 更新成功率 | ≥ 98% |
| 更新失败自动回滚 | ≥ 99.9% |
| 严重密钥泄露、安全越权和跨租户串用 | 0 |
| 未处理高危/严重安全问题 | 0 |

### 7.1 服务端 SLO

客户端指标不足以支撑灰度观察期判断，以下服务端指标同样作为 1.0 验收依据，并在 LH-V2-046
观察期持续量测：

| 类别 | 指标 |
|---|---:|
| Cloud API 月可用率 | ≥ 99.5% |
| `/v1/client/feature-policy` P95 延迟 | ≤ 300 ms |
| `/v1/client/feature-policy` 5xx 错误率 | ≤ 0.1% |
| runtime-config v1 兼容端点错误率 | ≤ 0.1%（旧客户端不可因新字段失败） |
| 模型代理首 token P95 | ≤ 3 s；请求失败率 ≤ 1% |
| Skill Catalog/安装类接口 P95 | ≤ 800 ms |
| Cloud Executor CloudRef Skill 成功率 | ≥ 99% |
| 紧急停用下发到服务端生效 | ≤ 5 s |
| Postgres 每日备份成功率 / RPO | ≥ 99%；RPO ≤ 24 h，RTO ≤ 2 h |
| 生产主机磁盘使用率告警线 | ≤ 80%（超过即造成发布阻断） |

## 八、每个任务的统一完成定义

任务标记“代码完成”前必须同时满足：

- 严格契约或 Schema 已冻结并拒绝未知字段。
- 单元测试覆盖成功、边界、失败和恢复路径。
- 涉及端云时完成 Memory、PostgreSQL 和真实 HTTP/IPC 契约测试。
- 涉及 OpenClaw 时完成锁定版真实 Gateway/Electron E2E 和兼容摘要更新。
- 涉及数据时说明存储位置、加密、保留、导出、删除和备份影响。
- 涉及权限时验证 UI 隐藏、直接 API、旧会话、离线缓存和撤销路径。
- README、DESIGN、OpenAPI/Core RPC、路线和验收记录同步。
- 不使用真实凭据、用户内容或生产私钥作为测试夹具。

### 8.1 回归套件分级与 CI 时长预算

0.5—0.8 每版都要真实 Gateway/Electron E2E，E2E 会随版本累积膨胀，因此回归套件必须分级并设时长上限：

| 分级 | 范围 | 触发时机 | 时长上限 |
|---|---|---|---|
| 单元 + 契约 | 全部包 | 每次提交 | ≤ 10 分钟 |
| 冒烟 E2E | 激活→聊天→Agent 切换→策略刷新→确认一次 | 每次 PR | ≤ 20 分钟 |
| 安全回归 | 策略绕过、确认重放、路径穿越、跨 Agent 隔离 | 每日 + 版本门禁 | ≤ 40 分钟 |
| 全量回归 | 历史累积的所有 E2E、安装/升级/回滚矩阵 | 版本候选门禁、每周一次 | ≤ 90 分钟 |

维护规则：

- 每个版本的“总门禁”任务（007/016/027/036）同时是当版回归套件的所有者，负责分级归类和时长治理。
- 新增功能默认只往安全回归或全量回归加用例；进入冒烟套件需同时从冒烟里移出或合并等价用例。
- 任何分级超时长上限时，当版不得新增 E2E，先做并行化、去重或降级，并在发布证据中记录。
- 不得为缩短时长删除安全类用例；安全回归只能增加不能减少。

候选、部署、发布和开放还必须分别附：

- 候选：构建、签名、安装、升级和回滚证据。
- 部署：迁移、健康、临时备份清理和回滚点。
- 发布：版本、大小、SHA-256、签名、渠道和公网验证。
- 开放：Feature Policy 版本、目标范围、观察指标和撤回方式。

## 九、风险登记

| 风险 | 影响 | 处理 | 阻断信号 |
|---|---|---|---|
| Feature Policy 只隐藏 UI | 高 | 服务端逐请求复验、deny-wins、紧急停用 | 直接 API 可使用关闭功能 |
| 产品入口扩大 OpenClaw 分叉 | 高 | Extension Surface、受限子窗口、兼容门禁 | 大量 DOM 业务逻辑或通用 IPC |
| Skill 安装变成任意代码 | 严重 | 三类安装语义、无原生入口、Worker allowlist | 未知实现可进入 Worker |
| 确认被模型伪造或重放 | 严重 | Core 生成、参数摘要、一次性消费 | 参数变化仍可复用确认 |
| 设备身份被当成自然人 | 高 | 1.0 个人内容本地化、审计 actor 标记 | 组织分享/高风险写入无用户身份 |
| 文件解析逃逸或资源耗尽 | 严重 | 隔离进程、资源上限、恶意样本门禁 | 解析器访问宿主或无限消耗 |
| 模型不支持媒体能力 | 中 | capability registry、关闭不支持入口 | 开关开启但真实请求必然失败 |
| 版本完成与线上开放混淆 | 高 | 八态模型、每次发布单独记录 | 勾选完成但用户实际不可用 |
| 1.x 功能拖延 1.0 | 高 | 1.0 范围冻结，媒体/连接器/自动化后移 | 0.9 继续加入新能力 |

## 十、复审结果

复审日期：2026-07-30。结论：计划逻辑通过，可以从 `LH-V2-001` 开始执行；外部商用材料继续保持
阻塞，不影响 0.5—0.8 的安全开发。

- [x] Feature Policy 在所有用户功能和 Skill UI 之前。
- [x] Feature Policy 使用独立端点，runtime-config v1 与 Desktop 0.4.1 保持兼容。
- [x] Product Extension Surface 在新增入口之前。
- [x] Confirmation Center 在任何写 Skill/Workflow 之前。
- [x] 1.0 Workflow 拒绝企业外部副作用，不依赖自然人审计。
- [x] 1.0 不依赖尚不存在的个人账号。
- [x] 个人知识在 1.0 明确为设备本地。
- [x] 本地官方 Skill 使用“启用”，不伪装为动态代码下载。
- [x] 会话操作禁止直接改写 OpenClaw 内部数据库或 transcript。
- [x] 0.5—0.8 每版只有一个主要产品目标。
- [x] 0.9 只做商用加固，不新增大功能。
- [x] 1.0 门槛明确覆盖 0.5—0.9。
- [x] 媒体、连接器、自动化和第三方执行已移到 1.x。
- [x] 38 项任务顺序连续、ID 唯一，且都有前置条件、状态和完成证据。
- [x] 外部证书、KMS、渗透测试和法务依赖保持“阻塞”，未伪造完成。
- [x] 0.5 标注可并行分组，关键路径不再全串行。
- [x] OpenClaw 会话 API 不确定性已前置为 0.5 的 `LH-V2-008` spike。
- [x] 外部阻断项已区分“启动时间”与“排期位置”。
- [x] 验收指标补齐服务端 SLO，灰度观察期有量化依据。
- [x] 回归套件已分级并设 CI 时长上限与维护责任人。

### 复审维度

| 维度 | 结果 | 说明 |
|---|---|---|
| 产品范围 | 通过 | 1.0 聚焦一键 Agent、官方 Skill、文件知识和无代码组合 |
| 依赖顺序 | 通过 | Policy → Extension → Confirmation → Skill → 文件知识 → Workflow |
| 向后兼容 | 通过 | 新增 Feature Policy 端点，不原地破坏 runtime-config v1 |
| 身份与审计 | 通过 | 1.0 个人数据本地化，企业外部写入延后到 Principal V2 |
| OpenClaw 边界 | 通过 | 主界面仍是 `/chat`，会话内部数据不直接改写 |
| Skill 供应链 | 通过 | 内置启用、声明安装、CloudRef 分离，任意代码继续禁止 |
| 版本范围 | 通过 | 0.5—0.8 单目标，0.9 只加固，1.x 承接扩展能力 |
| 验收可测性 | 通过 | 每项有证据，1.0 有客户端指标、服务端 SLO 和统一完成定义 |
| 工程可持续性 | 通过 | 回归分级与时长预算防止 E2E 无限膨胀 |

### 保留阻断

- `LH-V2-040`：可信 Authenticode 证书和正式品牌资产。
- `LH-V2-042`：生产 KMS 与密钥权限。
- `LH-V2-044`：独立渗透测试。
- `LH-V2-045`：法务与正式隐私/协议批准。

这些阻断被安排在 0.9，不应在材料未到位时提前标记完成，也不阻止从 0.5 平台基础开始开发。

## 十一、关键决策

| 日期 | 决策 | 理由 | 影响 |
|---|---|---|---|
| 2026-07-30 | V2 成为未来任务唯一进度源 | 避免 ROADMAP、功能策略和 Skill 文档各自排序 | 旧路线只保留历史 |
| 2026-07-30 | Feature Policy、扩展面和确认中心先行 | 所有后续功能共同依赖，晚做会返工 | 0.5 不追求新业务数量 |
| 2026-07-30 | 1.0 个人数据只在设备本地 | 当前只有设备授权，没有稳定用户身份 | 云端个人空间与分享进入 1.x |
| 2026-07-30 | 1.0 只开放声明式和官方能力 | Worker 仍是静态可信代码 | 第三方执行不阻塞商用 |
| 2026-07-30 | 0.9 冻结为商用加固 | 防止功能持续膨胀导致永远无法发布 | 媒体/连接器/自动化后移 |
| 2026-07-30 | 外部采购类项目立即启动 | 证书/KMS/渗测/法务周期以月计，等到 0.9 开始必延期 | 0.9 只做验收，不做采购等待 |
| 2026-07-30 | 会话 API 风险前置为 spike | 0.7 开头才发现需上游扩展会拖垮整版 | 0.5 并行调研，0.6 冻结前给出三态判定 |

## 十二、变更历史

### 2026-07-30 - 重建 V2 单一执行计划

**变更内容**：把功能开放、Skill、身份、UI 扩展、确认、文件、知识、无代码 Agent 和商用门禁重排为
0.5—1.0；新增 37 个有序任务、统一状态、完成定义、验收指标、风险和复审清单。

**变更理由**：旧计划存在 Feature Policy 排在 Skill UI 之后、0.7.1 范围过大、确认 UI 缺任务、个人
云能力缺身份前置，以及 0.7—0.9 与 1.0 门槛不一致的问题。

**影响范围**：后续产品、Desktop、Cloud API、Core、Skill/Pack Schema、OpenClaw 兼容、发布和运营。

**决策依据**：先完成所有能力的共同安全底座，再按“官方能力 → 文件知识 → 无代码组合 → 商用加固”
推进；非核心扩展不阻塞首个正式版。

### 2026-07-30 - V2 计划评审优化

**变更内容**：

1. 0.5 新增并行分组说明（策略线/扩展面线/确认线/调研线），关键路径不再全串行。
2. 新增 `LH-V2-008` OpenClaw 会话 API 可行性 spike，任务总数 37 → 38，0.6—0.9 顺序号顺延；
   `LH-V2-020` 前置改为 008 结论。
3. 新增 7.1 服务端 SLO（可用率、feature-policy 延迟/错误率、模型代理、备份 RPO/RTO、磁盘告警线）。
4. 新增 8.1 回归套件分级与 CI 时长预算，并指定各版总门禁任务为回归套件所有者。
5. 0.9 新增“启动时间 ≠ 排期位置”表，证书/KMS/法务立即启动，渗测在 0.7 期间预约档期。

**变更理由**：原计划存在四个可优化点：0.5 关键路径过度串行；0.7 最大不确定性（OpenClaw 会话
接口）未提前验证；验收指标只有客户端、缺服务端 SLO；E2E 累积没有时长预算和维护责任人；
以及外部采购周期被当成 0.9 才开始的工作。

**影响范围**：任务表顺序号、CI 配置与回归套件划分、可观测指标建设、采购与法务流程。

**运维先行处理（154.9.26.158）**：已清理 Docker 构建缓存/镜像与僵尸构建容器（磁盘 88% → 75%），
新增 2G swap，并上线生产 Postgres 每日 pg_dump（保留 14 天、含完整性校验），不再等到 `LH-V2-043`。
